<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://solahome.github.io</id>
    <title>sola的小屋</title>
    <updated>2025-04-11T15:12:10.531Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://solahome.github.io"/>
    <link rel="self" href="https://solahome.github.io/atom.xml"/>
    <subtitle>故事的结尾总写在开头</subtitle>
    <logo>https://solahome.github.io/images/avatar.png</logo>
    <icon>https://solahome.github.io/favicon.ico</icon>
    <rights>All rights reserved 2025, sola的小屋</rights>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(10)——ThreadPool类的实现(src/core/thread_pool.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-10/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-10/">
        </link>
        <updated>2025-04-05T09:06:43.000Z</updated>
        <content type="html"><![CDATA[<h1 id="threadpool-类代码结构与功能实现分析">ThreadPool 类代码结构与功能实现分析</h1>
<p><code>ThreadPool</code> 类是 InferLLM 框架中的线程池实现，用于并行执行计算任务，提高模型推理的性能。下面对 <code>thread_pool.h</code> 和 <code>thread_pool.cpp</code> 的代码结构和功能实现进行详细分析。</p>
<h2 id="1-infer_pause-宏定义">1. INFER_PAUSE 宏定义</h2>
<p>首先，框架定义了一个平台相关的 <code>INFER_PAUSE</code> 宏，用于实现 CPU 级别的暂停操作：</p>
<pre><code class="language-cpp">#ifndef INFER_PAUSE
# if defined __GNUC__ &amp;&amp; (defined __i386__ || defined __x86_64__)
#   if !defined(__SSE2__)
      static inline void non_sse_mm_pause() { __asm__ __volatile__ (&quot;rep; nop&quot;); }
#     define _mm_pause non_sse_mm_pause
#   else
#       include &lt;immintrin.h&gt;
#   endif
#   define INFER_PAUSE(v) do { for (int __delay = (v); __delay &gt; 0; --__delay) { _mm_pause(); } } while (0)
# elif defined __GNUC__ &amp;&amp; defined __aarch64__
#   define INFER_PAUSE(v) do { for (int __delay = (v); __delay &gt; 0; --__delay) { asm volatile(&quot;yield&quot; ::: &quot;memory&quot;); } } while (0)
# elif defined __GNUC__ &amp;&amp; defined __arm__
#   define INFER_PAUSE(v) do { for (int __delay = (v); __delay &gt; 0; --__delay) { asm volatile(&quot;&quot; ::: &quot;memory&quot;); } } while (0)
# elif defined __GNUC__ &amp;&amp; defined __riscv
#   define INFER_PAUSE(v) do { for (int __delay = (v); __delay &gt; 0; --__delay) { asm volatile(&quot;nop&quot;); } } while (0)
# else
#   warning &quot;Can't detect 'pause' (CPU-yield) instruction on the target platform. Specify INFER_PAUSE() definition via compiler flags.&quot;
#   define INFER_PAUSE(...) do { /* no-op: works, but not effective */ } while (0)
# endif
#endif // MTDA_PAUSE
</code></pre>
<p>这个宏根据不同的平台和架构，使用不同的指令实现 CPU 暂停操作，主要用于自旋锁中减少 CPU 资源消耗。</p>
<h2 id="2-worker-结构体">2. Worker 结构体</h2>
<p><code>Worker</code> 结构体表示一个工作线程及其状态：</p>
<pre><code class="language-cpp">struct Worker {
public:
    Worker(std::function&lt;void()&gt;&amp;&amp; run) : thread{run} {}
    ~Worker() { thread.join(); }
    //! Worker thread
    std::thread thread;
    //! Indicate whether the Worker thread need run
    std::atomic&lt;bool&gt; work_flag{false};
};
</code></pre>
<p>每个 <code>Worker</code> 包含一个线程和一个原子标志 <code>work_flag</code>，用于指示线程是否需要执行任务。</p>
<h2 id="3-threadpool-类">3. ThreadPool 类</h2>
<p><code>ThreadPool</code> 类是线程池的主要实现：</p>
<pre><code class="language-cpp">class ThreadPool {
public:
    //! Create thread-pool nr_threads thread_pool
    ThreadPool(uint32_t nr_threads);
    //! The main thread set the task, parallelism and worker flag to
    //! notify other thread.
    void add_task(const MultiThreadingTask&amp; task, uint32_t nr_task);

    inline void sync();
    //! wake up all the threads from cv.wait(), when the thread pool is not
    //! active, all the threads will go to sleep.
    inline void active();
    //! all the threads go to sleep which will reduce CPU occupation
    void deactive();
    ~ThreadPool();

    uint32_t nr_threads() const { return m_nr_threads; }

    //! The number of iterations &lt; main thread yeild resource&gt;
    static constexpr int MAIN_THREAD_ACTIVE_WAIT = 10000;
    //! The number of iterations &lt; worker thread yeild resource&gt;
    static constexpr int WORKER_ACTIVE_WAIT = 2000;
    //! The number of iterations &lt;pause&gt;
    static constexpr int ACTIVE_WAIT_PAUSE_LIMIT = 16;

private:
    uint32_t m_nr_threads = 1;
    //! All the sub task number
    uint32_t m_nr_task = 0;
    uint32_t m_task_per_thread = 0;
    std::atomic_bool m_stop{false};
    std::atomic_bool m_active{false};
    //! The executable funcition pointer
    MultiThreadingTask m_task;

    std::vector&lt;Worker*&gt; m_workers;
    //! The cv and mutex for threading activity
    std::condition_variable m_cv;
    std::mutex m_mutex;
};
</code></pre>
<h3 id="31-构造函数">3.1 构造函数</h3>
<pre><code class="language-cpp">ThreadPool::ThreadPool(uint32_t threads_num)
        : m_nr_threads(threads_num), m_stop{false}, m_active{false} {
    if (threads_num &lt; 1) {
        m_nr_threads = 1;
    }
    if (m_nr_threads &gt; 1) {
        auto system_cpu_count = std::thread::hardware_concurrency();
        if (m_nr_threads &gt; system_cpu_count) {
            INFER_LOG(
                    &quot;The number of threads is bigger than number of &quot;
                    &quot;physical cpu cores, got: %d core_number: %d&quot;,
                    system_cpu_count, nr_threads());
        }
        for (uint32_t i = 0; i &lt; m_nr_threads - 1; i++) {
            m_workers.push_back(new Worker([this, i]() {
                while (!m_stop) {
                    while (m_active) {
                        //! if the thread should work
                        if (m_workers[i]-&gt;work_flag.load(std::memory_order_acquire)) {
                            m_task(TaskId{
                                    i * m_task_per_thread,
                                    std::min((i + 1) * m_task_per_thread, m_nr_task),
                                    i});
                            //! Flag worker is finished
                            m_workers[i]-&gt;work_flag.store(
                                    false, std::memory_order_release);
                        }
                        //! Wait next task coming
                        for (int it = 0; it &lt; WORKER_ACTIVE_WAIT; it++) {
                            if (m_workers[i]-&gt;work_flag.load(
                                        std::memory_order_acquire)) {
                                break;
                            }
                            if (it &lt; ACTIVE_WAIT_PAUSE_LIMIT || (it &amp; 1)) {
                                INFER_PAUSE(16);  // Spin lock's CPU-level yield
                            } else {
                                // Spin lock's OS-level yield
                                std::this_thread::yield();
                            }
                        }
                    }
                    {
                        std::unique_lock&lt;std::mutex&gt; lock(m_mutex);
                        if (!m_stop &amp;&amp; !m_active) {
                            m_cv.wait(lock, [this] { return m_stop || m_active; });
                        }
                    }
                }
            }));
        }
    }
}
</code></pre>
<p>构造函数创建指定数量的工作线程：</p>
<ol>
<li>确保线程数至少为 1</li>
<li>检查线程数是否超过系统 CPU 核心数，如果超过则发出警告</li>
<li>创建 <code>m_nr_threads - 1</code> 个工作线程（主线程也会参与计算）</li>
<li>每个工作线程的主循环：
<ul>
<li>外层循环：当 <code>m_stop</code> 为 false 时持续运行</li>
<li>中层循环：当 <code>m_active</code> 为 true 时处于活动状态</li>
<li>内层逻辑：
<ul>
<li>如果 <code>work_flag</code> 为 true，执行任务并将 <code>work_flag</code> 设为 false</li>
<li>使用自旋等待检查是否有新任务</li>
<li>如果超过等待限制，则使用 <code>std::this_thread::yield()</code> 让出 CPU</li>
</ul>
</li>
<li>当 <code>m_active</code> 为 false 时，使用条件变量等待唤醒</li>
</ul>
</li>
</ol>
<h3 id="32-add_task-方法">3.2 add_task 方法</h3>
<pre><code class="language-cpp">void ThreadPool::add_task(const MultiThreadingTask&amp; task, uint32_t nr_task) {
    //! If only one thread or one task, execute directly
    if (m_nr_threads == 1 || nr_task == 1) {
        task({0, nr_task, m_nr_threads - 1});
        return;
    } else {
        active();
        INFER_ASSERT(m_active, &quot;thread pool is not actived.&quot;);
        m_nr_task = nr_task;
        //! Set the task number, task iter and task
        m_task_per_thread = (nr_task + m_nr_threads - 1) / m_nr_threads;
        m_task = std::move(task);
        for (uint32_t i = 0; i &lt; m_nr_threads - 1; i++) {
            m_workers[i]-&gt;work_flag.store(true, std::memory_order_release);
        }
        //! Main thread working
        uint32_t start = (m_nr_threads - 1) * m_task_per_thread;
        // printf(&quot;main threads start\n&quot;);
        m_task({start, nr_task, m_nr_threads - 1});
        //! make sure all threads done
        sync();
    }
}
</code></pre>
<p><code>add_task</code> 方法向线程池添加任务：</p>
<ol>
<li>如果只有一个线程或一个任务，直接在当前线程执行</li>
<li>否则：
<ul>
<li>激活线程池</li>
<li>设置任务数量和每个线程的任务数</li>
<li>设置任务函数</li>
<li>将所有工作线程的 <code>work_flag</code> 设为 true，通知它们开始工作</li>
<li>主线程也参与计算，处理剩余的任务</li>
<li>调用 <code>sync()</code> 等待所有线程完成任务</li>
</ul>
</li>
</ol>
<h3 id="33-sync-方法">3.3 sync 方法</h3>
<pre><code class="language-cpp">inline void ThreadPool::sync() {
    bool no_finished = false;
    uint32_t no_finished_id = 0;
    do {
        no_finished = false;
        for (uint32_t i = no_finished_id; i &lt; m_nr_threads - 1; ++i) {
            if (m_workers[i]-&gt;work_flag.load(std::memory_order_acquire)) {
                no_finished = true;
                no_finished_id = i;
                break;
            }
        }
        if (no_finished) {
            for (int it = 0; it &lt; MAIN_THREAD_ACTIVE_WAIT; it++) {
                if (!m_workers[no_finished_id]-&gt;work_flag.load(
                            std::memory_order_acquire)) {
                    break;
                }
                if ((it &lt; ACTIVE_WAIT_PAUSE_LIMIT || (it &amp; 1))) {
                    INFER_PAUSE(16);
                } else {
                    std::this_thread::yield();
                }
            }
        }
    } while (no_finished);
}
</code></pre>
<p><code>sync</code> 方法等待所有工作线程完成任务：</p>
<ol>
<li>检查是否有未完成的线程（<code>work_flag</code> 为 true）</li>
<li>如果有，使用自旋等待该线程完成</li>
<li>如果超过等待限制，则使用 <code>std::this_thread::yield()</code> 让出 CPU</li>
<li>重复上述过程，直到所有线程完成任务</li>
</ol>
<h3 id="34-active-和-deactive-方法">3.4 active 和 deactive 方法</h3>
<pre><code class="language-cpp">inline void ThreadPool::active() {
    if (!m_active) {
        std::unique_lock&lt;std::mutex&gt; lock(m_mutex);
        m_active = true;
        m_cv.notify_all();
    }
}

void ThreadPool::deactive() {
    std::unique_lock&lt;std::mutex&gt; lock(m_mutex);
    m_active = false;
}
</code></pre>
<ul>
<li><code>active</code> 方法激活线程池，唤醒所有等待的工作线程</li>
<li><code>deactive</code> 方法停用线程池，使工作线程进入休眠状态，减少 CPU 占用</li>
</ul>
<h3 id="35-析构函数">3.5 析构函数</h3>
<pre><code class="language-cpp">ThreadPool::~ThreadPool() {
    {
        std::unique_lock&lt;std::mutex&gt; lock(m_mutex);
        m_stop = true;
        m_active = false;
        m_cv.notify_all();
    }
    for (auto&amp; worker : m_workers) {
        delete worker;
    }
}
</code></pre>
<p>析构函数清理线程池资源：</p>
<ol>
<li>设置 <code>m_stop</code> 为 true，通知所有工作线程退出</li>
<li>唤醒所有等待的工作线程</li>
<li>删除所有工作线程对象</li>
</ol>
<h2 id="4-任务分配策略">4. 任务分配策略</h2>
<p>ThreadPool 使用简单的任务分配策略：</p>
<ol>
<li>将总任务数 <code>nr_task</code> 平均分配给所有线程</li>
<li>每个线程处理 <code>m_task_per_thread = (nr_task + m_nr_threads - 1) / m_nr_threads</code> 个任务</li>
<li>第 i 个线程处理的任务范围是 <code>[i * m_task_per_thread, min((i + 1) * m_task_per_thread, m_nr_task)]</code></li>
<li>主线程（最后一个线程）处理剩余的任务</li>
</ol>
<h2 id="5-同步机制">5. 同步机制</h2>
<p>ThreadPool 使用多种同步机制：</p>
<ol>
<li>
<p><strong>原子变量</strong>：</p>
<ul>
<li><code>m_stop</code>：指示线程池是否停止</li>
<li><code>m_active</code>：指示线程池是否处于活动状态</li>
<li><code>work_flag</code>：指示工作线程是否需要执行任务</li>
</ul>
</li>
<li>
<p><strong>条件变量</strong>：</p>
<ul>
<li><code>m_cv</code>：用于在线程池激活时唤醒工作线程</li>
</ul>
</li>
<li>
<p><strong>自旋锁</strong>：</p>
<ul>
<li>使用 <code>INFER_PAUSE</code> 和 <code>std::this_thread::yield()</code> 实现自旋等待，减少线程切换开销</li>
</ul>
</li>
</ol>
<h2 id="6-优化策略">6. 优化策略</h2>
<p>ThreadPool 实现了几种优化策略：</p>
<ol>
<li>
<p><strong>主线程参与计算</strong>：<br>
主线程也参与任务计算，减少线程创建和切换开销。</p>
</li>
<li>
<p><strong>自旋等待</strong>：<br>
使用自旋等待检查任务状态，减少线程切换开销。自旋等待分为两个阶段：</p>
<ul>
<li>前 <code>ACTIVE_WAIT_PAUSE_LIMIT</code> 次迭代使用 CPU 级别的暂停操作</li>
<li>之后交替使用 CPU 级别的暂停和 OS 级别的让出</li>
</ul>
</li>
<li>
<p><strong>线程休眠</strong>：<br>
当线程池不活动时，工作线程进入休眠状态，减少 CPU 占用。</p>
</li>
<li>
<p><strong>任务合并</strong>：<br>
当只有一个线程或一个任务时，直接在当前线程执行，避免线程调度开销。</p>
</li>
<li>
<p><strong>动态休眠</strong>：<br>
线程池可以通过 <code>deactive()</code> 方法进入休眠状态，在不需要并行计算时减少 CPU 占用。</p>
</li>
<li>
<p><strong>快速检测</strong>：<br>
在 <code>sync()</code> 方法中，记录上一个未完成的线程 ID，优先检查该线程，减少不必要的检查。</p>
</li>
</ol>
<h2 id="7-使用示例">7. 使用示例</h2>
<p>ThreadPool 的典型使用方式如下：</p>
<pre><code class="language-cpp">// 创建线程池
ThreadPool pool(4);  // 4个线程

// 定义任务函数
auto task = [&amp;](const TaskId&amp; task_id) {
    // task_id.start: 任务起始索引
    // task_id.end: 任务结束索引
    // task_id.thread_id: 线程ID
    for (uint32_t i = task_id.start; i &lt; task_id.end; ++i) {
        // 处理第i个任务
    }
};

// 添加任务
pool.add_task(task, 100);  // 100个任务

// 停用线程池（不需要并行计算时）
pool.deactive();

// 激活线程池（需要并行计算时）
pool.active();
</code></pre>
<h2 id="8-taskid-结构体">8. TaskId 结构体</h2>
<pre><code class="language-cpp">struct TaskId {
    uint32_t start;     // 任务起始索引
    uint32_t end;       // 任务结束索引
    uint32_t thread_id; // 线程ID
};
</code></pre>
<p><code>TaskId</code> 结构体用于描述任务的范围和执行线程，它包含三个字段：</p>
<ul>
<li><code>start</code>：任务的起始索引</li>
<li><code>end</code>：任务的结束索引</li>
<li><code>thread_id</code>：执行任务的线程ID</li>
</ul>
<h2 id="9-multithreadingtask-类型">9. MultiThreadingTask 类型</h2>
<pre><code class="language-cpp">using MultiThreadingTask = std::function&lt;void(const TaskId&amp;)&gt;;
</code></pre>
<p><code>MultiThreadingTask</code> 是一个函数类型，表示可以并行执行的任务。它接受一个 <code>TaskId</code> 参数，用于指定任务的范围和执行线程。</p>
<h2 id="10-线程池的生命周期管理">10. 线程池的生命周期管理</h2>
<p>ThreadPool 的生命周期管理非常重要，它涉及到线程的创建、激活、停用和销毁：</p>
<ol>
<li>
<p><strong>创建</strong>：<br>
在构造函数中创建工作线程，但线程初始处于非活动状态。</p>
</li>
<li>
<p><strong>激活</strong>：<br>
调用 <code>active()</code> 方法激活线程池，唤醒所有工作线程。</p>
</li>
<li>
<p><strong>停用</strong>：<br>
调用 <code>deactive()</code> 方法停用线程池，使工作线程进入休眠状态。</p>
</li>
<li>
<p><strong>销毁</strong>：<br>
在析构函数中设置 <code>m_stop</code> 为 true，通知所有工作线程退出，然后等待线程结束并释放资源。</p>
</li>
</ol>
<h2 id="11-与其他组件的交互">11. 与其他组件的交互</h2>
<p>ThreadPool 主要与 CPUDevice 类交互，用于并行执行 CPU 上的计算任务：</p>
<pre><code class="language-cpp">// 在 CPUDevice 中创建线程池
m_thread_pool = std::make_unique&lt;ThreadPool&gt;(nr_thread);

// 使用线程池执行并行任务
m_thread_pool-&gt;add_task([&amp;](const TaskId&amp; task_id) {
    // 执行计算任务
}, total_tasks);
</code></pre>
<h2 id="总结">总结</h2>
<p>ThreadPool 类是 InferLLM 框架中的线程池实现，用于并行执行计算任务，提高模型推理的性能。它采用了多种优化策略，如主线程参与计算、自旋等待、线程休眠和任务合并等，以减少线程调度开销并提高并行效率。它还提供了灵活的接口，支持动态激活和停用线程池，以适应不同的计算需求。</p>
<p>通过分析 <code>thread_pool.h</code> 和 <code>thread_pool.cpp</code>，我们可以看到 InferLLM 框架在并行计算方面的设计思想和实现细节，这对于理解框架的性能优化策略非常有帮助。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(09)——ModelImp类的实现(src/core/model_imp.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-09/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-09/">
        </link>
        <updated>2025-04-03T08:21:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="modelimp-类代码结构与功能实现分析">ModelImp 类代码结构与功能实现分析</h1>
<p><code>ModelImp</code> 类是 InferLLM 框架中 <code>Model</code> 类的具体实现，负责模型的加载、初始化和推理过程。下面对 <code>model_imp.h</code> 和 <code>model_imp.cpp</code> 的代码结构和功能实现进行详细分析。</p>
<h2 id="1-辅助函数">1. 辅助函数</h2>
<pre><code class="language-cpp">DType dtype_from_str(const std::string&amp; str) {
    if (str == &quot;float32&quot; || str == &quot;fp23&quot;) {
        return DType::Float32;
    } else if (str == &quot;float16&quot; || str == &quot;fp16&quot;) {
        return DType::Float16;
    } else if (str == &quot;int8&quot;) {
        return DType::Int8;
    } else if (str == &quot;uint8&quot;) {
        return DType::Uint8;
    } else if (str == &quot;int4&quot;) {
        return DType::Int4;
    } else if (str == &quot;uint4&quot;) {
        return DType::Uint4;
    } else {
        INFER_ASSERT(0, &quot;Unsupported dytpe.&quot;);
    }
}
</code></pre>
<p>这个辅助函数将字符串表示的数据类型转换为枚举类型 <code>DType</code>，支持多种精度的数据类型，包括 float32、float16、int8、uint8、int4 和 uint4。</p>
<h2 id="2-modelimp-类">2. ModelImp 类</h2>
<h3 id="21-构造函数">2.1 构造函数</h3>
<pre><code class="language-cpp">ModelImp::ModelImp(const ModelConfig&amp; config, const std::string&amp; name)
        : m_name(name), m_config(config) {
    uint32_t nr_thread = config.nr_thread;
    std::string device_type = config.device_type;
    if (device_type == &quot;CPU&quot; || device_type == &quot;cpu&quot;) {
#if INFER_X86
        m_device = make_unique&lt;CPUDevice&gt;(KernelType::X86, nr_thread);
#elif INFER_ARM
        m_device = make_unique&lt;CPUDevice&gt;(KernelType::Arm, nr_thread);
#else
        m_device = make_unique&lt;CPUDevice&gt;(KernelType::Naive, nr_thread);
#endif
    } else if (
            device_type == &quot;GPU&quot; || device_type == &quot;CUDA&quot; || device_type == &quot;gpu&quot;) {
        // if compile with GPU, use GPU, else use CPUDevice
#if ENABLE_GPU
        m_device = make_unique&lt;GPUDevice&gt;(0);
#else
        INFER_ASSERT(0, &quot;GPU is disabled when build, please build with GPU.&quot;);
#endif
    }

    UserConfig user_config;
    user_config.compt_type = dtype_from_str(config.compt_type);
    m_graph = Graph::make_graph(user_config, m_device.get(), name);
    m_past = 0;
}
</code></pre>
<p>构造函数根据配置创建相应的设备和计算图：</p>
<ol>
<li>根据 <code>device_type</code> 创建 CPU 或 GPU 设备
<ul>
<li>对于 CPU，根据编译选项选择 X86、Arm 或 Naive 内核</li>
<li>对于 GPU，检查是否启用了 GPU 支持</li>
</ul>
</li>
<li>创建用户配置，设置计算类型</li>
<li>创建计算图</li>
<li>初始化 <code>m_past</code> 为 0，表示已处理的 token 数量</li>
</ol>
<h3 id="22-load-方法">2.2 load 方法</h3>
<pre><code class="language-cpp">void ModelImp::load(const std::string&amp; model_path) {
    m_vocab = std::make_shared&lt;Vocab&gt;();
    std::shared_ptr&lt;InputFile&gt; fin =
            std::make_shared&lt;InputFile&gt;(model_path, m_config.enable_mmap);

    m_param.n_ctx = m_config.nr_ctx;
    m_graph-&gt;load(fin, m_param, m_vocab);
    m_logist.resize(m_param.n_vocab);
}
</code></pre>
<p><code>load</code> 方法从指定路径加载模型：</p>
<ol>
<li>创建词汇表对象</li>
<li>创建输入文件对象，支持内存映射</li>
<li>设置上下文长度</li>
<li>调用计算图的 <code>load</code> 方法加载模型</li>
<li>调整 logits 向量的大小为词汇表大小</li>
</ol>
<h3 id="23-init-方法">2.3 init 方法</h3>
<pre><code class="language-cpp">void init(
        uint32_t top_k, float top_p, float temp, float repeat_penalty,
        int repeat_last_n, int32_t seed, int32_t end_token) {
    m_top_k = top_k;
    m_top_p = top_p;
    m_temp = temp;
    m_repeat_penalty = repeat_penalty;
    m_repeat_last_n = repeat_last_n;
    m_end_token = end_token;
    for (uint32_t i = 0; i &lt; m_repeat_last_n; i++) {
        m_last_queue.push_back(0);
    }
    m_rng = std::mt19937(seed);
}
</code></pre>
<p><code>init</code> 方法初始化模型的生成参数：</p>
<ol>
<li>设置采样参数：top_k、top_p、温度、重复惩罚等</li>
<li>设置结束 token</li>
<li>初始化最近生成的 token 队列</li>
<li>使用指定的种子初始化随机数生成器</li>
</ol>
<h3 id="24-prefill-方法">2.4 prefill 方法</h3>
<pre><code class="language-cpp">void ModelImp::prefill(const std::string&amp; promote) {
    auto tokens = tokenize(promote, true);
    m_graph-&gt;post_tokenize(tokens);
    for (auto token : tokens) {
        m_last_queue.push_back(token);
        m_last_queue.pop_front();
    }
    m_graph-&gt;execute(tokens, m_logist, m_past, true);
    m_past = tokens.size();
}
</code></pre>
<p><code>prefill</code> 方法处理初始提示文本：</p>
<ol>
<li>将文本分词为 token 序列</li>
<li>调用计算图的 <code>post_tokenize</code> 方法进行后处理</li>
<li>更新最近生成的 token 队列</li>
<li>执行计算图，进行预填充（prefill=true）</li>
<li>更新已处理的 token 数量</li>
</ol>
<h3 id="25-decode-方法">2.5 decode 方法</h3>
<pre><code class="language-cpp">std::string ModelImp::decode(const std::string&amp; user_input, int&amp; token) {
    auto tokens = tokenize(user_input, false);
    m_graph-&gt;post_tokenize(tokens);
    for (auto token : tokens) {
        m_last_queue.push_back(token);
        m_last_queue.pop_front();
    }
    m_graph-&gt;execute(tokens, m_logist, m_past, false);
    sample_and_update();
    m_past += tokens.size();
    token = m_pre_token;
    return m_vocab-&gt;id_to_token[m_pre_token].tok;
}
</code></pre>
<p><code>decode</code> 方法处理用户输入并生成下一个 token：</p>
<ol>
<li>将用户输入分词为 token 序列</li>
<li>调用计算图的 <code>post_tokenize</code> 方法进行后处理</li>
<li>更新最近生成的 token 队列</li>
<li>执行计算图，生成 logits</li>
<li>采样下一个 token 并更新状态</li>
<li>更新已处理的 token 数量</li>
<li>返回生成的 token 对应的文本</li>
</ol>
<h3 id="26-decode_iter-方法">2.6 decode_iter 方法</h3>
<pre><code class="language-cpp">std::string ModelImp::decode_iter(int&amp; token) {
    auto start = m_timer.get_time();
    m_graph-&gt;execute({m_pre_token}, m_logist, m_past);
    auto end = m_timer.get_time();
    m_time_cost += end - start;
    sample_and_update();
    m_past++;
    token = m_pre_token;
    return m_vocab-&gt;id_to_token[m_pre_token].tok;
}
</code></pre>
<p><code>decode_iter</code> 方法迭代生成下一个 token：</p>
<ol>
<li>记录开始时间</li>
<li>执行计算图，使用上一个生成的 token 作为输入</li>
<li>记录结束时间并更新总计算时间</li>
<li>采样下一个 token 并更新状态</li>
<li>更新已处理的 token 数量</li>
<li>返回生成的 token 对应的文本</li>
</ol>
<h3 id="27-sample_and_update-方法">2.7 sample_and_update 方法</h3>
<pre><code class="language-cpp">int32_t ModelImp::sample_and_update() {
    // sample the next token
   auto token = llama_sample_top_p_top_k(
           *m_vocab, m_logist.data(), m_last_queue, m_repeat_penalty, m_top_k, m_top_p,
           m_temp, m_rng);
    // update the last queue
    m_last_queue.push_back(token);
    m_last_queue.pop_front();
    m_pre_token = token;
    if (token == m_end_token) {
        m_device-&gt;deactive();
    }
    return token;
}
</code></pre>
<p><code>sample_and_update</code> 方法采样下一个 token 并更新状态：</p>
<ol>
<li>使用 top-p 和 top-k 采样方法从 logits 中采样下一个 token</li>
<li>更新最近生成的 token 队列</li>
<li>更新上一个生成的 token</li>
<li>如果生成的 token 是结束 token，则停用设备</li>
<li>返回生成的 token</li>
</ol>
<h3 id="28-tokenize-方法">2.8 tokenize 方法</h3>
<pre><code class="language-cpp">std::vector&lt;Vocab::Id&gt; ModelImp::tokenize(const std::string&amp; text, bool bos) {
    std::vector&lt;Vocab::Id&gt; res;
    std::vector&lt;int&gt; score;
    std::vector&lt;Vocab::Id&gt; prev;
    int len = text.length();

    score.resize(len + 1);
    prev.resize(len + 1);

    // Forward pass
    for (int i = 0; i &lt; len; i++) {
        int max_len = std::min(len - i, MAX_TOKEN_LEN);
        for (int sub_len = 1; sub_len &lt;= len - i; sub_len++) {
            auto sub = text.substr(i, sub_len);
            auto token = m_vocab-&gt;token_to_id.find(sub);
            if (token != m_vocab-&gt;token_to_id.end()) {
                int token_score = sub.length() * sub.length();
                int local_score = score[i] + token_score;
                int next = i + sub_len;
                if (score[next] &lt; local_score) {
                    score[next] = local_score;
                    prev[next] = (*token).second;
                }
            }
        }
    }

    // Backward pass
    int i = len;
    while (i &gt; 0) {
        Vocab::Id token_id = prev[i];
        if (token_id == 0) {
            // TODO: Return error or something more meaningful
            printf(&quot;failed to tokenize string!\n&quot;);
            break;
        }
        res.push_back(token_id);
        auto token = m_vocab-&gt;id_to_token[token_id].tok;
        i -= token.length();
    }

    if (bos) {
        res.push_back(1);  // TODO: replace with vocab.bos
    }

    // Pieces are in reverse order so correct that
    std::reverse(res.begin(), res.end());

    return res;
}
</code></pre>
<p><code>tokenize</code> 方法将文本分词为 token 序列：</p>
<ol>
<li>使用动态规划算法找到最优的分词方式
<ul>
<li>前向传递：计算每个位置的最优分数和对应的 token</li>
<li>后向传递：从末尾开始，根据前向传递的结果构建 token 序列</li>
</ul>
</li>
<li>如果需要，添加 BOS（Beginning of Sequence）token</li>
<li>反转 token 序列，使其按正确的顺序排列</li>
<li>返回 token 序列</li>
</ol>
<p>这个分词算法使用了贪婪的方法，尽量选择长度更长的 token，以减少序列长度。</p>
<h3 id="29-decode_summary-方法">2.9 decode_summary 方法</h3>
<pre><code class="language-cpp">std::string ModelImp::decode_summary() const {
    std::string ret = &quot;Run Model Summary:\n&quot;;
    ret += &quot;Total Model Compute Time:   &quot; + std::to_string(m_time_cost) + &quot;s\n&quot;;
    ret += &quot;Total Model Compute Token:  &quot; + std::to_string(m_past) + &quot;\n&quot;;
    ret += &quot;Average Token Compute Time: &quot; +
           std::to_string(m_time_cost * 1000 / m_past) + &quot;ms\n&quot;;
    ret += &quot;Average Token Generation Speed: &quot; +
           std::to_string(m_past / m_time_cost) + &quot;token/s\n&quot;;
    return ret;
}
</code></pre>
<p><code>decode_summary</code> 方法返回模型运行的摘要信息：</p>
<ol>
<li>总计算时间</li>
<li>总计算 token 数量</li>
<li>平均 token 计算时间</li>
<li>平均 token 生成速度</li>
</ol>
<h2 id="3-工作流程">3. 工作流程</h2>
<p><code>ModelImp</code> 类的典型工作流程如下：</p>
<ol>
<li>
<p><strong>创建和初始化</strong>：</p>
<pre><code class="language-cpp">ModelImp model_imp(config, &quot;llama&quot;);
model_imp.load(&quot;path/to/model.bin&quot;);
model_imp.init(40, 0.9f, 0.8f, 1.1f, 64, 42, -1);
</code></pre>
</li>
<li>
<p><strong>预填充</strong>：</p>
<pre><code class="language-cpp">model_imp.prefill(&quot;你好，请介绍一下自己。&quot;);
</code></pre>
</li>
<li>
<p><strong>迭代生成</strong>：</p>
<pre><code class="language-cpp">int token;
std::string result;
while (true) {
    std::string next = model_imp.decode_iter(token);
    if (token == end_token) break;
    result += next;
}
</code></pre>
</li>
<li>
<p><strong>获取摘要</strong>：</p>
<pre><code class="language-cpp">std::string summary = model_imp.decode_summary();
</code></pre>
</li>
</ol>
<h2 id="4-关键数据结构">4. 关键数据结构</h2>
<p><code>ModelImp</code> 类包含以下关键数据成员：</p>
<ul>
<li>
<p><strong>设备和计算图</strong>：</p>
<ul>
<li><code>m_device</code>：计算设备（CPU 或 GPU）</li>
<li><code>m_graph</code>：计算图，负责模型的结构和执行</li>
</ul>
</li>
<li>
<p><strong>模型参数</strong>：</p>
<ul>
<li><code>m_param</code>：LLM 模型参数</li>
<li><code>m_config</code>：用户配置</li>
<li><code>m_vocab</code>：词汇表</li>
</ul>
</li>
<li>
<p><strong>生成参数</strong>：</p>
<ul>
<li><code>m_top_k</code>、<code>m_top_p</code>、<code>m_temp</code>：采样参数</li>
<li><code>m_repeat_penalty</code>、<code>m_repeat_last_n</code>：重复惩罚参数</li>
<li><code>m_end_token</code>：结束 token</li>
</ul>
</li>
<li>
<p><strong>状态变量</strong>：</p>
<ul>
<li><code>m_past</code>：已处理的 token 数量</li>
<li><code>m_pre_token</code>：上一个生成的 token</li>
<li><code>m_last_queue</code>：最近生成的 token 队列，用于重复惩罚</li>
<li><code>m_logist</code>：模型输出的 logits，表示下一个 token 的概率分布</li>
</ul>
</li>
<li>
<p><strong>性能统计</strong>：</p>
<ul>
<li><code>m_timer</code>：计时器，用于测量计算时间</li>
<li><code>m_time_cost</code>：总计算时间</li>
</ul>
</li>
</ul>
<h2 id="5-采样算法">5. 采样算法</h2>
<p><code>ModelImp</code> 类使用 <code>llama_sample_top_p_top_k</code> 函数进行采样，这是一个结合了 top-k 和 top-p（nucleus sampling）的采样方法：</p>
<pre><code class="language-cpp">int32_t llama_sample_top_p_top_k(
        const Vocab&amp; vocab, float* logits, const std::list&lt;int32_t&gt;&amp; last_n_tokens,
        float repeat_penalty, int top_k, float top_p, float temp, std::mt19937&amp; rng) {
    // 应用重复惩罚
    for (auto token_id : last_n_tokens) {
        logits[token_id] /= repeat_penalty;
    }

    // 应用温度
    for (int i = 0; i &lt; vocab.id_to_token.size(); i++) {
        logits[i] /= temp;
    }

    // 计算 softmax
    float max_logit = -INFINITY;
    for (int i = 0; i &lt; vocab.id_to_token.size(); i++) {
        max_logit = std::max(max_logit, logits[i]);
    }
    
    float sum = 0.0f;
    for (int i = 0; i &lt; vocab.id_to_token.size(); i++) {
        logits[i] = expf(logits[i] - max_logit);
        sum += logits[i];
    }
    
    for (int i = 0; i &lt; vocab.id_to_token.size(); i++) {
        logits[i] /= sum;
    }

    // 找出 top-k 的 token
    std::vector&lt;std::pair&lt;float, int&gt;&gt; candidates;
    for (int i = 0; i &lt; vocab.id_to_token.size(); i++) {
        candidates.push_back({logits[i], i});
    }
    
    std::partial_sort(
            candidates.begin(), candidates.begin() + top_k, candidates.end(),
            [](const std::pair&lt;float, int&gt;&amp; a, const std::pair&lt;float, int&gt;&amp; b) {
                return a.first &gt; b.first;
            });
    
    candidates.resize(top_k);

    // 应用 top-p（nucleus sampling）
    float cumsum = 0.0f;
    for (int i = 0; i &lt; candidates.size(); i++) {
        cumsum += candidates[i].first;
        if (cumsum &gt;= top_p) {
            candidates.resize(i + 1);
            break;
        }
    }

    // 按概率采样
    std::vector&lt;float&gt; probs;
    for (auto&amp; candidate : candidates) {
        probs.push_back(candidate.first);
    }
    
    std::discrete_distribution&lt;&gt; dist(probs.begin(), probs.end());
    return candidates[dist(rng)].second;
}
</code></pre>
<p>这个采样算法的步骤如下：</p>
<ol>
<li>应用重复惩罚，降低最近生成的 token 的概率</li>
<li>应用温度参数，控制分布的平滑程度</li>
<li>计算 softmax，将 logits 转换为概率</li>
<li>选择概率最高的 top-k 个 token</li>
<li>应用 top-p 采样，只保留累积概率达到 p 的 token</li>
<li>按概率采样最终的 token</li>
</ol>
<h2 id="6-分词算法">6. 分词算法</h2>
<p><code>ModelImp</code> 类使用动态规划算法进行分词，尽量选择长度更长的 token：</p>
<pre><code class="language-cpp">std::vector&lt;Vocab::Id&gt; ModelImp::tokenize(const std::string&amp; text, bool bos) {
    std::vector&lt;Vocab::Id&gt; res;
    std::vector&lt;int&gt; score;
    std::vector&lt;Vocab::Id&gt; prev;
    int len = text.length();

    score.resize(len + 1);
    prev.resize(len + 1);

    // 前向传递：计算每个位置的最优分数和对应的 token
    for (int i = 0; i &lt; len; i++) {
        int max_len = std::min(len - i, MAX_TOKEN_LEN);
        for (int sub_len = 1; sub_len &lt;= len - i; sub_len++) {
            auto sub = text.substr(i, sub_len);
            auto token = m_vocab-&gt;token_to_id.find(sub);
            if (token != m_vocab-&gt;token_to_id.end()) {
                int token_score = sub.length() * sub.length();
                int local_score = score[i] + token_score;
                int next = i + sub_len;
                if (score[next] &lt; local_score) {
                    score[next] = local_score;
                    prev[next] = (*token).second;
                }
            }
        }
    }

    // 后向传递：从末尾开始，根据前向传递的结果构建 token 序列
    int i = len;
    while (i &gt; 0) {
        Vocab::Id token_id = prev[i];
        if (token_id == 0) {
            printf(&quot;failed to tokenize string!\n&quot;);
            break;
        }
        res.push_back(token_id);
        auto token = m_vocab-&gt;id_to_token[token_id].tok;
        i -= token.length();
    }

    if (bos) {
        res.push_back(1);  // BOS token
    }

    // 反转 token 序列，使其按正确的顺序排列
    std::reverse(res.begin(), res.end());

    return res;
}
</code></pre>
<p>这个分词算法的特点是：</p>
<ol>
<li>使用动态规划找到最优的分词方式</li>
<li>优先选择长度更长的 token，以减少序列长度</li>
<li>支持添加 BOS（Beginning of Sequence）token</li>
</ol>
<h2 id="7-优化策略">7. 优化策略</h2>
<p><code>ModelImp</code> 类实现了几种优化策略：</p>
<ol>
<li>
<p><strong>预填充模式</strong>：</p>
<pre><code class="language-cpp">m_graph-&gt;execute(tokens, m_logist, m_past, true);
</code></pre>
<p>预填充模式可以一次性处理多个 token，提高效率。</p>
</li>
<li>
<p><strong>延迟加载</strong>：</p>
<pre><code class="language-cpp">m_graph-&gt;load(fin, m_param, m_vocab);
</code></pre>
<p>模型权重采用延迟加载策略，只在需要时才从文件读取。</p>
</li>
<li>
<p><strong>性能统计</strong>：</p>
<pre><code class="language-cpp">auto start = m_timer.get_time();
// ...
auto end = m_timer.get_time();
m_time_cost += end - start;
</code></pre>
<p>记录计算时间，用于性能分析和优化。</p>
</li>
<li>
<p><strong>设备管理</strong>：</p>
<pre><code class="language-cpp">if (token == m_end_token) {
    m_device-&gt;deactive();
}
</code></pre>
<p>在生成结束时停用设备，释放资源。</p>
</li>
</ol>
<h2 id="8-与其他组件的交互">8. 与其他组件的交互</h2>
<p><code>ModelImp</code> 类与框架的其他组件有密切的交互：</p>
<ol>
<li>
<p><strong>与 Device 的交互</strong>：</p>
<pre><code class="language-cpp">m_device = make_unique&lt;CPUDevice&gt;(KernelType::X86, nr_thread);
</code></pre>
<p>创建并管理计算设备，负责内存分配和计算执行。</p>
</li>
<li>
<p><strong>与 Graph 的交互</strong>：</p>
<pre><code class="language-cpp">m_graph = Graph::make_graph(user_config, m_device.get(), name);
m_graph-&gt;load(fin, m_param, m_vocab);
m_graph-&gt;execute(tokens, m_logist, m_past, prefill);
</code></pre>
<p>创建并管理计算图，负责模型的结构和执行。</p>
</li>
<li>
<p><strong>与 Vocab 的交互</strong>：</p>
<pre><code class="language-cpp">m_vocab = std::make_shared&lt;Vocab&gt;();
auto tokens = tokenize(promote, true);
</code></pre>
<p>创建并管理词汇表，负责文本和 token 的转换。</p>
</li>
</ol>
<h2 id="总结">总结</h2>
<p><code>ModelImp</code> 类是 InferLLM 框架的核心实现类，它负责模型的加载、初始化和推理过程。它通过与 Device、Graph 和 Vocab 等组件的交互，实现了高效的大型语言模型推理。它支持多种优化策略，如预填充模式、延迟加载和性能统计，以提高推理效率。它还实现了先进的采样算法和分词算法，以提高生成文本的质量。</p>
<p>通过分析 <code>model_imp.h</code> 和 <code>model_imp.cpp</code>，我们可以看到 InferLLM 框架的设计思想和实现细节，这对于理解和使用框架非常有帮助。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(08)——Model类的实现(src/core/model.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-08/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-08/">
        </link>
        <updated>2025-04-01T07:41:22.000Z</updated>
        <content type="html"><![CDATA[<h1 id="model-类代码结构与功能实现分析">Model 类代码结构与功能实现分析</h1>
<p><code>Model</code> 类是 InferLLM 框架的对外接口层，它封装了底层模型实现的细节，提供了一套简洁的 API 供用户使用。下面对 <code>model.h</code> 和 <code>model.cpp</code> 的代码结构和功能实现进行详细分析。</p>
<h2 id="1-api-宏定义">1. API 宏定义</h2>
<pre><code class="language-cpp">#if defined(_WIN32)
#define API __declspec(dllexport)
#else
#define API __attribute__((visibility(&quot;default&quot;)))
#endif
</code></pre>
<p>这个宏定义用于控制函数和类的导出属性，确保在不同平台上正确导出符号：</p>
<ul>
<li>在 Windows 平台上使用 <code>__declspec(dllexport)</code></li>
<li>在其他平台上使用 <code>__attribute__((visibility(&quot;default&quot;)))</code></li>
</ul>
<h2 id="2-modelconfig-结构体">2. ModelConfig 结构体</h2>
<pre><code class="language-cpp">struct ModelConfig {
    //! dtype include 'float32','float16','int8','int4'
    std::string compt_type = &quot;float32&quot;;
    //! device_type include 'cpu','gpu'
    std::string device_type = &quot;cpu&quot;;
    uint32_t nr_thread;
    uint32_t nr_ctx;
    int32_t device_id;
    bool enable_mmap;
};
</code></pre>
<p><code>ModelConfig</code> 结构体定义了模型的配置参数：</p>
<ul>
<li><code>compt_type</code>：计算类型，支持 &quot;float32&quot;、&quot;float16&quot;、&quot;int8&quot;、&quot;int4&quot;</li>
<li><code>device_type</code>：设备类型，支持 &quot;cpu&quot;、&quot;gpu&quot;</li>
<li><code>nr_thread</code>：线程数量</li>
<li><code>nr_ctx</code>：上下文长度</li>
<li><code>device_id</code>：设备 ID（对于 GPU）</li>
<li><code>enable_mmap</code>：是否启用内存映射</li>
</ul>
<h2 id="3-model-类">3. Model 类</h2>
<p><code>Model</code> 类是框架的主要接口类，它使用了桥接模式，将具体实现委托给 <code>ModelImp</code> 类：</p>
<pre><code class="language-cpp">class API Model {
public:
    //! 构造函数，创建指定类型的模型
    Model(const ModelConfig&amp; config, const std::string&amp; model_name);

    //! 加载模型
    void load(const std::string&amp; model_path);

    //! 初始化模型参数
    void init(uint32_t top_k, float top_p, float temp, float repeat_penalty,
              int repeat_last_n, int32_t seed, int32_t end_token);

    //! 获取剩余 token 数量
    uint32_t get_remain_token();

    //! 重置 token
    void reset_token();

    //! 预填充模型
    void prefill(const std::string&amp; promote);

    //! 解码（带用户输入）
    std::string decode(const std::string&amp; user_input, int&amp; token);

    //! 迭代解码
    std::string decode_iter(int&amp; token);

    //! 获取解码摘要
    std::string decode_summary() const;

private:
    std::shared_ptr&lt;ModelImp&gt; m_model_imp;
};
</code></pre>
<h3 id="31-构造函数">3.1 构造函数</h3>
<pre><code class="language-cpp">Model::Model(const ModelConfig&amp; config, const std::string&amp; model_name) {
    //! TODO: create the model implement by the model name
    m_model_imp = std::make_shared&lt;ModelImp&gt;(config, model_name);
}
</code></pre>
<p>构造函数接收模型配置和模型名称，创建对应的 <code>ModelImp</code> 实例。注释中的 TODO 表明这里未来可能会根据模型名称创建不同的实现类。</p>
<h3 id="32-load-方法">3.2 load 方法</h3>
<pre><code class="language-cpp">void Model::load(const std::string&amp; model_path) {
    m_model_imp-&gt;load(model_path);
}
</code></pre>
<p><code>load</code> 方法从指定路径加载模型文件，它直接调用 <code>ModelImp</code> 的 <code>load</code> 方法。</p>
<h3 id="33-init-方法">3.3 init 方法</h3>
<pre><code class="language-cpp">void Model::init(
        uint32_t top_k, float top_p, float temp, float repeat_penalty,
        int repeat_last_n, int32_t seed, int32_t end_token) {
    m_model_imp-&gt;init(
            top_k, top_p, temp, repeat_penalty, repeat_last_n, seed, end_token);
}
</code></pre>
<p><code>init</code> 方法初始化模型的生成参数：</p>
<ul>
<li><code>top_k</code>：保留概率最高的 k 个 token</li>
<li><code>top_p</code>：保留累积概率达到 p 的 token</li>
<li><code>temp</code>：温度参数，控制生成的随机性</li>
<li><code>repeat_penalty</code>：重复惩罚，降低已生成 token 的概率</li>
<li><code>repeat_last_n</code>：考虑最后 n 个 token 进行重复惩罚</li>
<li><code>seed</code>：随机数种子</li>
<li><code>end_token</code>：结束 token</li>
</ul>
<h3 id="34-token-管理方法">3.4 token 管理方法</h3>
<pre><code class="language-cpp">uint32_t Model::get_remain_token() {
    return m_model_imp-&gt;get_remain_token();
}

void Model::reset_token() {
    return m_model_imp-&gt;reset_token();
}
</code></pre>
<p>这两个方法用于管理 token：</p>
<ul>
<li><code>get_remain_token</code>：获取剩余可生成的 token 数量</li>
<li><code>reset_token</code>：重置 token 计数，通常在开始新的生成任务时调用</li>
</ul>
<h3 id="35-生成方法">3.5 生成方法</h3>
<pre><code class="language-cpp">void Model::prefill(const std::string&amp; promote) {
    return m_model_imp-&gt;prefill(promote);
}

std::string Model::decode(const std::string&amp; user_input, int&amp; token) {
    return m_model_imp-&gt;decode(user_input, token);
}

std::string Model::decode_iter(int&amp; token) {
    return m_model_imp-&gt;decode_iter(token);
}

std::string Model::decode_summary() const {
    return m_model_imp-&gt;decode_summary();
}
</code></pre>
<p>这些方法用于文本生成：</p>
<ul>
<li><code>prefill</code>：预填充模型，处理初始提示文本</li>
<li><code>decode</code>：解码用户输入，生成下一个 token</li>
<li><code>decode_iter</code>：迭代解码，生成下一个 token（不需要用户输入）</li>
<li><code>decode_summary</code>：获取生成摘要，通常包含生成统计信息</li>
</ul>
<h2 id="4-桥接模式的应用">4. 桥接模式的应用</h2>
<p><code>Model</code> 类采用了桥接模式，将接口与实现分离：</p>
<ul>
<li><code>Model</code> 类提供稳定的对外接口</li>
<li><code>ModelImp</code> 类负责具体实现</li>
<li>这种设计使得可以在不改变接口的情况下，更换或升级底层实现</li>
</ul>
<p>桥接模式的优点在这里得到了充分体现：</p>
<ol>
<li><strong>接口稳定</strong>：用户代码只需要依赖 <code>Model</code> 类的接口，不需要关心具体实现</li>
<li><strong>实现可替换</strong>：可以根据不同的模型类型或硬件平台，提供不同的 <code>ModelImp</code> 实现</li>
<li><strong>隐藏复杂性</strong>：复杂的模型加载、推理逻辑都封装在 <code>ModelImp</code> 中，对用户透明</li>
</ol>
<h2 id="5-工作流程">5. 工作流程</h2>
<p>使用 <code>Model</code> 类的典型工作流程如下：</p>
<ol>
<li>
<p><strong>创建模型</strong>：</p>
<pre><code class="language-cpp">ModelConfig config;
config.compt_type = &quot;float32&quot;;
config.device_type = &quot;cpu&quot;;
config.nr_thread = 4;
config.nr_ctx = 2048;
config.enable_mmap = true;

Model model(config, &quot;llama&quot;);
</code></pre>
</li>
<li>
<p><strong>加载模型</strong>：</p>
<pre><code class="language-cpp">model.load(&quot;path/to/model.bin&quot;);
</code></pre>
</li>
<li>
<p><strong>初始化参数</strong>：</p>
<pre><code class="language-cpp">model.init(40, 0.9f, 0.8f, 1.1f, 64, 42, -1);
</code></pre>
</li>
<li>
<p><strong>预填充模型</strong>：</p>
<pre><code class="language-cpp">model.prefill(&quot;你好，请介绍一下自己。&quot;);
</code></pre>
</li>
<li>
<p><strong>迭代生成</strong>：</p>
<pre><code class="language-cpp">int token;
std::string result;
while (true) {
    std::string next = model.decode_iter(token);
    if (token == end_token) break;
    result += next;
}
</code></pre>
</li>
</ol>
<h2 id="总结">总结</h2>
<p><code>Model</code> 类是 InferLLM 框架的对外接口层，它通过桥接模式封装了底层模型实现的细节，提供了一套简洁、稳定的 API 供用户使用。它支持模型加载、参数配置和文本生成等核心功能，使用户可以方便地使用大型语言模型进行推理。</p>
<p>通过分析 <code>model.h</code> 和 <code>model.cpp</code>，我们可以看到 InferLLM 框架采用了良好的设计模式和接口抽象，使得框架具有良好的可扩展性和可维护性。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(07)——KvStorage类的实现(src/core/kvstorage.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-07/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-07/">
        </link>
        <updated>2025-03-30T07:35:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="kvstorage-类代码结构与功能实现分析">KvStorage 类代码结构与功能实现分析</h1>
<p><code>KvStorage</code> 类是 InferLLM 框架中用于存储注意力机制中的 Key 和 Value 缓存的特殊张量类。它继承自 <code>Tensor</code> 类，并提供了动态扩展内存的能力，以适应长序列生成过程中不断增长的 KV 缓存需求。</p>
<h2 id="1-kvstorageconfig-类">1. KvStorageConfig 类</h2>
<p>首先，框架定义了一个单例类 <code>KvStorageConfig</code>，用于管理 KV 缓存的全局配置：</p>
<pre><code class="language-cpp">class KvStorageConfig {
public:
    constexpr static uint32_t START_KV_INDEX = 100;  // 初始 KV 缓存大小
    constexpr static uint32_t KV_STEP = 100;         // KV 缓存扩展步长
    static std::shared_ptr&lt;KvStorageConfig&gt; instance;
    
    // 单例模式获取实例
    static std::shared_ptr&lt;KvStorageConfig&gt; get_instance() {
        if (instance == nullptr) {
            instance = std::make_shared&lt;KvStorageConfig&gt;();
        }
        return instance;
    }
    
    // 增加 KV 缓存计数
    uint32_t increase_count() {
        m_kv_count++;
        return m_kv_count;
    }
    
    // 获取起始索引
    uint32_t get_start_index() { return START_KV_INDEX + m_kv_count * 2; }
    
    // 获取当前计数
    uint32_t get_count() { return m_kv_count; }
    
private:
    uint32_t m_kv_count = 0;  // KV 缓存计数
};
</code></pre>
<p>这个类使用单例模式，确保全局只有一个实例，用于跟踪和管理所有 KV 缓存的分配。它定义了两个关键常量：</p>
<ul>
<li><code>START_KV_INDEX</code>：初始 KV 缓存大小，设为 100</li>
<li><code>KV_STEP</code>：KV 缓存扩展步长，也设为 100</li>
</ul>
<h2 id="2-kvstorage-类">2. KvStorage 类</h2>
<p><code>KvStorage</code> 类继承自 <code>Tensor</code> 类，并添加了管理 KV 缓存的特殊功能：</p>
<pre><code class="language-cpp">class KvStorage : public Tensor {
public:
    // 构造函数
    KvStorage(std::vector&lt;size_t&gt; shape, DType dtype, Device* device);
    
    // 析构函数
    ~KvStorage() {
        auto data = ptr();
        device()-&gt;aligned_free(data);
    }
    
    // 获取当前数据指针
    void* get_current_data();
    
    // 设置共享内存
    void set_shared_memory(void* data, size_t length = 0) override;
    
    // 准备指定长度的数据
    TensorState prepare_data_with_length(uint32_t len);
    
    // 增加索引
    size_t add_id(uint32_t id);
    
    // 获取当前索引
    size_t current_index() const { return m_store_id; }
    
    // 重置索引
    void reset_id();
    
private:
    size_t m_store_id;    // 当前存储索引
    size_t m_total_id;    // 总索引数
    uint32_t m_curr_id;   // 当前分配的索引数
    void* m_curr_data;    // 当前数据指针
    uint32_t m_kv_id;     // KV 缓存 ID
};
</code></pre>
<h3 id="21-构造函数">2.1 构造函数</h3>
<pre><code class="language-cpp">KvStorage::KvStorage(std::vector&lt;size_t&gt; shape, DType dtype, Device* device)
        : Tensor(device, &quot;kvstorage&quot;) {
    m_store_id = 0;
    m_total_id = shape[0];
    m_kv_id = KvStorageConfig::get_instance()-&gt;increase_count();
    m_curr_id = KvStorageConfig::get_instance()-&gt;get_start_index();
    //! only allocate the memory of length m_curr_id * embd
    shape[0] = m_curr_id;
    set_shape(shape, dtype);
    size_t len = length_in_byte();
    //! no need use memory pool
    auto data = device-&gt;aligned_alloc(len);
    set_shared_memory(data, len);
}
</code></pre>
<p>构造函数初始化了 KV 缓存的关键参数：</p>
<ol>
<li>设置当前存储索引 <code>m_store_id</code> 为 0</li>
<li>记录总索引数 <code>m_total_id</code></li>
<li>从 <code>KvStorageConfig</code> 获取一个新的 KV 缓存 ID</li>
<li>获取初始分配的索引数 <code>m_curr_id</code></li>
<li>调整形状，只分配初始需要的内存</li>
<li>使用 <code>aligned_alloc</code> 分配对齐内存</li>
<li>设置为共享内存</li>
</ol>
<p>注意，这里没有使用设备的内存池，而是直接分配对齐内存，这是因为 KV 缓存需要长期存在，不适合使用内存池管理。</p>
<h3 id="22-set_shared_memory-方法">2.2 set_shared_memory 方法</h3>
<pre><code class="language-cpp">void KvStorage::set_shared_memory(void* data, size_t size) {
    Tensor::set_shared_memory(data, size);
    m_curr_data =
            static_cast&lt;char*&gt;(ptr()) +
            static_cast&lt;size_t&gt;((stride()[0] * m_store_id * dtype_in_byte(dtype())));
}
</code></pre>
<p>这个方法重写了 <code>Tensor</code> 类的 <code>set_shared_memory</code> 方法，除了设置共享内存外，还更新了当前数据指针 <code>m_curr_data</code>。</p>
<h3 id="23-prepare_data_with_length-方法">2.3 prepare_data_with_length 方法</h3>
<pre><code class="language-cpp">TensorState KvStorage::prepare_data_with_length(uint32_t len) {
    Tensor::prepare_data();
    //! if memory is not enough, allocate a new memory and copy the data to the new
    if (m_store_id + len &gt;= m_curr_id) {
        auto shape = this-&gt;shape();
        shape[0] = m_curr_id + KvStorageConfig::KV_STEP;
        size_t old_len = length_in_byte();
        void* old_ptr = ptr();

        set_shape(shape, dtype());
        size_t len = length_in_byte();
        auto data = device()-&gt;aligned_alloc(len);
        device()-&gt;device2device_copy(data, old_ptr, old_len);

        device()-&gt;aligned_free(old_ptr);

        set_shared_memory(data, len);
        m_curr_id += KvStorageConfig::KV_STEP;
    }
    m_curr_data =
            static_cast&lt;char*&gt;(ptr()) +
            static_cast&lt;size_t&gt;((stride()[0] * m_store_id * dtype_in_byte(dtype())));
    return TensorState::Own;
}
</code></pre>
<p>这是 <code>KvStorage</code> 类的核心方法，它实现了动态扩展内存的功能：</p>
<ol>
<li>首先调用基类的 <code>prepare_data</code> 方法确保数据已准备好</li>
<li>检查当前内存是否足够存储新的数据</li>
<li>如果内存不足，则：
<ul>
<li>计算新的形状，增加 <code>KV_STEP</code> 个索引</li>
<li>记录旧内存的长度和指针</li>
<li>更新形状</li>
<li>分配新的内存</li>
<li>将旧内存的数据复制到新内存</li>
<li>释放旧内存</li>
<li>设置新的共享内存</li>
<li>更新当前分配的索引数</li>
</ul>
</li>
<li>更新当前数据指针</li>
<li>返回 <code>TensorState::Own</code> 表示数据已准备好</li>
</ol>
<p>这个方法确保了 KV 缓存可以在需要时动态扩展，而不会丢失已有的数据。</p>
<h3 id="24-其他方法">2.4 其他方法</h3>
<pre><code class="language-cpp">void* KvStorage::get_current_data() {
    INFER_ASSERT(
            is_own(),
            &quot;The Kvstorage is not ready, please call prepare_data ahead.&quot;);
    m_curr_data = static_cast&lt;char*&gt;(ptr()) +
                  static_cast&lt;size_t&gt;(
                          (stride()[0] * m_store_id * dtype_in_byte(dtype())));
    return m_curr_data;
}
</code></pre>
<p><code>get_current_data</code> 方法返回当前数据指针，它首先检查数据是否已准备好，然后计算当前数据的位置并返回。</p>
<pre><code class="language-cpp">size_t KvStorage::add_id(uint32_t id) {
    INFER_ASSERT(id + m_store_id &lt; m_total_id, &quot;KvStorage add id error!&quot;);
    m_store_id += id;
    m_curr_data = static_cast&lt;char*&gt;(ptr()) +
                  static_cast&lt;size_t&gt;(
                          (stride()[0] * m_store_id * dtype_in_byte(dtype())));
    return m_store_id;
}
</code></pre>
<p><code>add_id</code> 方法增加当前存储索引，并更新当前数据指针。它首先检查增加后的索引是否超过总索引数，然后更新索引和数据指针。</p>
<pre><code class="language-cpp">void KvStorage::reset_id() {
    m_store_id = 0;
    m_curr_data = ptr();
}
</code></pre>
<p><code>reset_id</code> 方法重置当前存储索引和数据指针，通常在处理新的序列时调用。</p>
<h2 id="3-kvstorage-的工作流程">3. KvStorage 的工作流程</h2>
<p>KvStorage 的典型工作流程如下：</p>
<ol>
<li>
<p><strong>创建 KvStorage</strong>：</p>
<pre><code class="language-cpp">auto kv_cache = std::make_shared&lt;KvStorage&gt;(shape, dtype, device);
</code></pre>
<p>这会分配初始大小的内存。</p>
</li>
<li>
<p><strong>准备数据</strong>：</p>
<pre><code class="language-cpp">kv_cache-&gt;prepare_data_with_length(len);
</code></pre>
<p>这会确保有足够的内存存储新的数据，必要时会扩展内存。</p>
</li>
<li>
<p><strong>获取当前数据指针</strong>：</p>
<pre><code class="language-cpp">auto data = kv_cache-&gt;get_current_data();
</code></pre>
<p>这会返回当前数据的指针，用于读写数据。</p>
</li>
<li>
<p><strong>增加索引</strong>：</p>
<pre><code class="language-cpp">kv_cache-&gt;add_id(1);
</code></pre>
<p>这会增加当前存储索引，通常在处理完一个 token 后调用。</p>
</li>
<li>
<p><strong>重置索引</strong>：</p>
<pre><code class="language-cpp">kv_cache-&gt;reset_id();
</code></pre>
<p>这会重置当前存储索引，通常在处理新的序列时调用。</p>
</li>
</ol>
<h2 id="4-kvstorage-的优化策略">4. KvStorage 的优化策略</h2>
<p>KvStorage 实现了几种优化策略：</p>
<ol>
<li><strong>延迟分配</strong>：初始只分配一部分内存，随着需求增加再扩展</li>
<li><strong>内存对齐</strong>：使用对齐内存分配，提高内存访问效率</li>
<li><strong>内存复用</strong>：在扩展内存时，复制已有数据，避免重新计算</li>
<li><strong>索引管理</strong>：通过索引管理数据位置，避免不必要的内存移动</li>
</ol>
<h2 id="总结">总结</h2>
<p>KvStorage 类是 InferLLM 框架中用于管理注意力机制 KV 缓存的特殊张量类。它通过动态扩展内存的方式，支持长序列生成过程中不断增长的 KV 缓存需求。它的设计考虑了内存效率和访问效率，是框架中实现高效推理的关键组件之一。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(06)——Graph类的实现(src/core/graph.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-06/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-06/">
        </link>
        <updated>2025-03-27T07:31:09.000Z</updated>
        <content type="html"><![CDATA[<h1 id="graph-类代码结构与功能实现分析">Graph 类代码结构与功能实现分析</h1>
<p><code>Graph</code> 类是 InferLLM 框架中的核心组件，负责构建和执行模型的计算图。下面对 <code>graph.h</code> 和 <code>graph.cpp</code> 的代码结构和功能实现进行详细分析。</p>
<h2 id="1-核心数据结构">1. 核心数据结构</h2>
<h3 id="11-llmparams-结构体">1.1 LlmParams 结构体</h3>
<pre><code class="language-cpp">struct LlmParams {
    bool is_multi_query = false;
    int32_t multi_query_group_num = 1;
    int32_t n_vocab;        // 词汇表大小
    int32_t n_embd;         // 嵌入维度
    int32_t n_mult;         // 中间层倍数
    int32_t n_head;         // 注意力头数
    int32_t n_layer;        // 层数
    int32_t n_rot;          // 旋转位置编码维度
    int32_t ftype;          // 权重类型
    int32_t n_ctx;          // 上下文长度
};
</code></pre>
<p>这个结构体存储了 LLM 模型的核心参数，包括模型结构信息和计算配置。</p>
<h3 id="12-userconfig-结构体">1.2 UserConfig 结构体</h3>
<pre><code class="language-cpp">struct UserConfig {
    DType compt_type;       // 计算类型
};
</code></pre>
<p>这个结构体存储了用户配置的计算类型。</p>
<h2 id="2-oprmodulebase-类">2. OprModuleBase 类</h2>
<p><code>OprModuleBase</code> 是所有操作模块的基类，封装了一组相关的算子：</p>
<pre><code class="language-cpp">class OprModuleBase {
public:
    // 构造函数
    OprModuleBase(std::shared_ptr&lt;Tensor&gt; input, Device* device, const std::string&amp; name);
    OprModuleBase(std::vector&lt;std::shared_ptr&lt;Tensor&gt;&gt; inputs, Device* device, const std::string&amp; name);
    
    // 核心方法
    size_t get_workspace_in_byte();
    void deduce_output_shape();
    virtual void execute(WorkSpace* workspace, uint32_t nr_past, bool is_prefill = false);
    
    // 添加算子
    template &lt;typename Op, typename... Args&gt;
    std::vector&lt;std::shared_ptr&lt;Tensor&gt;&gt; add_opr(Args&amp;&amp;... args);
    
    // 获取权重
    std::vector&lt;std::shared_ptr&lt;Tensor&gt;&gt; get_all_weights();
    
    // 输入输出管理
    std::vector&lt;std::shared_ptr&lt;Tensor&gt;&gt; inputs() const;
    std::shared_ptr&lt;Tensor&gt; input(int id = 0) const;
    std::shared_ptr&lt;Tensor&gt; output() const;
    
    // 重置上下文
    virtual void reset_ctx() {}
};
</code></pre>
<p><code>OprModuleBase</code> 的主要功能：</p>
<ol>
<li>管理一组相关的算子</li>
<li>提供统一的执行接口</li>
<li>管理输入输出张量</li>
<li>计算工作空间需求</li>
</ol>
<h3 id="21-execute-方法实现">2.1 execute 方法实现</h3>
<pre><code class="language-cpp">void OprModuleBase::execute(WorkSpace* workspace, uint32_t nr_past, bool) {
    for (auto opr : m_oprs) {
        opr-&gt;pre_execute();
#ifdef INFER_PROFILE
        struct timeval start, end;
        gettimeofday(&amp;start, NULL);
#endif
        opr-&gt;execute(workspace, nr_past);

#ifdef INFER_PROFILE
        gettimeofday(&amp;end, NULL);
        long seconds = end.tv_sec - start.tv_sec;
        float micros = (seconds * 1000) + (float)(end.tv_usec - start.tv_usec) / 1000;
        printf(&quot;Op %s spent time %f ms\n&quot;, opr-&gt;name().c_str(), micros);
#endif
        opr-&gt;end_execute();
    }
}
</code></pre>
<p>这个方法按顺序执行模块中的所有算子，并支持性能分析。</p>
<h2 id="3-特定模块实现">3. 特定模块实现</h2>
<p>框架定义了多种特定的模块，用于构建不同类型的模型：</p>
<h3 id="31-attentionmodule">3.1 AttentionModule</h3>
<pre><code class="language-cpp">template &lt;typename Attention&gt;
class AttentionModule : public OprModuleBase {
    // 构造函数中构建注意力计算图
    AttentionModule(...) {
        // 创建注意力算子
        m_attention_op = std::make_shared&lt;Attention&gt;(...);
        oprs().push_back(m_attention_op);
        
        // 创建投影算子
        auto proj_out = add_opr&lt;MatMul&gt;(...)[0];
        set_output(proj_out);
    }
    
    // 重置上下文
    void reset_ctx() override { m_attention_op-&gt;reset_ctx(); }
};
</code></pre>
<p>这是一个模板类，可以使用不同的注意力实现（如 LlamaAttention、GlmAttention 等）。</p>
<h3 id="32-ffn-模块">3.2 FFN 模块</h3>
<p>框架实现了多种 FFN（前馈网络）模块，适用于不同的模型架构：</p>
<h4 id="llamaffnmodule">LlamaFFNModule</h4>
<pre><code class="language-cpp">LlamaFFNModule::LlamaFFNModule(...) {
    size_t nff = ((2 * (4 * embd) / 3 + mult - 1) / mult) * mult;
    // 创建第一个矩阵乘法
    auto matmul_out0 = add_opr&lt;MatMul&gt;(...)[0];
    // 创建第二个矩阵乘法
    auto matmul_out1 = add_opr&lt;MatMul&gt;(...)[0];
    // SiLU 激活函数
    auto silu_out = add_opr&lt;Elemwise&gt;(..., ElemMode::Silu)[0];
    // 元素乘法
    auto mul_out = add_opr&lt;Elemwise&gt;(..., ElemMode::Mul)[0];
    // 最后的矩阵乘法
    auto matmul_out2 = add_opr&lt;MatMul&gt;(...)[0];
    set_output(matmul_out2);
}
</code></pre>
<h4 id="glmffnmodule">GlmFFNModule</h4>
<pre><code class="language-cpp">GlmFFNModule::GlmFFNModule(...) {
    // 矩阵乘法
    auto matmul_out1 = add_opr&lt;MatMul&gt;(..., true)[0];
    // GELU 激活函数
    auto gelu_out = add_opr&lt;Elemwise&gt;(..., ElemMode::Gelu)[0];
    // 矩阵乘法
    auto matmul_out2 = add_opr&lt;MatMul&gt;(..., true)[0];
    set_output(matmul_out2);
}
</code></pre>
<h4 id="glm2ffnmodule">Glm2FFNModule</h4>
<pre><code class="language-cpp">Glm2FFNModule::Glm2FFNModule(...) {
    // 矩阵乘法
    auto matmul_out1 = add_opr&lt;MatMul&gt;(..., false)[0];
    // SpliteHalfActiveMul 操作
    auto gelu_out = add_opr&lt;SpliteHalfActiveMul&gt;(..., ElemMode::Silu)[0];
    // 矩阵乘法
    auto matmul_out2 = add_opr&lt;MatMul&gt;(..., false)[0];
    set_output(matmul_out2);
}
</code></pre>
<h3 id="33-headmodule">3.3 HeadModule</h3>
<pre><code class="language-cpp">HeadModule::HeadModule(...) {
    // 层归一化
    auto norm_out = add_opr&lt;LayerNorm&gt;(...)[0];
    // 矩阵乘法（输出层）
    auto matmul_out = add_opr&lt;MatMulLast&gt;(...)[0];
    set_output(matmul_out);
}

void HeadModule::execute(WorkSpace* workspace, uint32_t nr_past, bool is_prefill) {
    // 预填充模式下不执行
    if (!is_prefill) {
        for (auto opr : oprs()) {
            opr-&gt;pre_execute();
            opr-&gt;execute(workspace, nr_past);
            opr-&gt;end_execute();
        }
    }
}
</code></pre>
<p><code>HeadModule</code> 实现了模型的输出头，将隐藏状态映射到词汇表空间。它有一个特殊的 <code>execute</code> 方法，在预填充模式下不执行。</p>
<h3 id="34-embdmodule">3.4 EmbdModule</h3>
<pre><code class="language-cpp">EmbdModule::EmbdModule(...) {
    // 嵌入层
    auto embd_out = add_opr&lt;Embedding&gt;(...)[0];
    set_output(embd_out);
}
</code></pre>
<p><code>EmbdModule</code> 实现了模型的嵌入层，将输入 token 映射到嵌入空间。</p>
<h3 id="35-oneopmodule">3.5 OneOpModule</h3>
<pre><code class="language-cpp">template &lt;class Op&gt;
class OneOpModule : public OprModuleBase {
    // 添加单个算子
    template &lt;typename... Args&gt;
    std::shared_ptr&lt;Tensor&gt; add_opr(Args&amp;&amp;... args) {
        auto opr = std::make_shared&lt;Op&gt;(
                device(), name(), inputs(), std::forward&lt;Args&gt;(args)...);
        oprs().push_back(opr);
        set_output(opr-&gt;outputs()[0]);
        return opr-&gt;outputs()[0];
    }
};
</code></pre>
<p><code>OneOpModule</code> 是一个辅助类，用于创建只包含一个算子的模块。</p>
<h2 id="4-graph-类">4. Graph 类</h2>
<p><code>Graph</code> 类是整个计算图的管理者，负责构建模型结构、加载权重和执行推理：</p>
<pre><code class="language-cpp">class Graph : public std::enable_shared_from_this&lt;Graph&gt; {
public:
    // 构造函数
    Graph(UserConfig model_config, Device* device, const std::string&amp; name);
    
    // 静态工厂方法
    static std::shared_ptr&lt;Graph&gt; make_graph(UserConfig model_config, Device* device, const std::string&amp; name);
    
    // 执行方法
    void execute(std::vector&lt;int32_t&gt; in_token, std::vector&lt;float&gt;&amp; logist, uint32_t nr_past, bool prefill = false);
    
    // 添加模块
    template &lt;typename OpModule, typename... Args&gt;
    std::shared_ptr&lt;Tensor&gt; add_module(Args&amp;&amp;... args);
    
    // 重置上下文
    void reset_ctx();
    
    // 收集权重
    void collect_weights();
    
    // 加载模型
    virtual void load(std::shared_ptr&lt;InputFile&gt; fin, LlmParams&amp; param, std::shared_ptr&lt;Vocab&gt; vocab);
    
    // 构建模型结构（由派生类实现）
    virtual void construct_llm() = 0;
};
</code></pre>
<h3 id="41-execute-方法实现">4.1 execute 方法实现</h3>
<pre><code class="language-cpp">void Graph::execute(std::vector&lt;int32_t&gt; in_token, std::vector&lt;float&gt;&amp; logist, uint32_t nr_past, bool prefill) {
    // 检查输入形状，必要时重新分配工作空间
    if (m_input-&gt;dims() == 0 || !same_input_shape(in_token)) {
        m_input-&gt;set_shape({in_token.size()}, DType::Int32);
        size_t len = get_workspace_in_byte();
        if(m_workspace-&gt;ptr() == nullptr) {
            auto data = m_device-&gt;allocate(len);
            m_workspace-&gt;set_memory(data, len);
        } else if (m_workspace-&gt;ptr() &amp;&amp; len &gt; m_workspace-&gt;length()) {
            m_device-&gt;free_device(m_workspace-&gt;ptr());
            auto data = m_device-&gt;allocate(len);
            m_workspace-&gt;set_memory(data, len);
        }
    }
    
    // 准备输入数据
    m_input-&gt;resume_user_count();
    m_input-&gt;prepare_data();
    m_device-&gt;host2device_copy(m_input-&gt;ptr(), in_token.data(), in_token.size() * sizeof(int32_t), true);
    
    // 执行所有模块
    for (size_t i = 0; i &lt; m_modules.size(); i++) {
        m_modules[i]-&gt;execute(m_workspace.get(), nr_past, prefill);
    }
    
    // 如果不是预填充模式，复制输出数据
    if (!prefill) {
        m_device-&gt;device2host_copy(logist.data(), m_output-&gt;ptr(), logist.size() * sizeof(float), true);
    }
    
    // 同步设备并释放输出内存
    m_device-&gt;sync();
    m_output-&gt;recall_data();
}
</code></pre>
<p>这个方法是模型推理的核心，它执行以下步骤：</p>
<ol>
<li>检查输入形状，必要时重新分配工作空间</li>
<li>准备输入数据</li>
<li>按顺序执行所有模块</li>
<li>复制输出数据（非预填充模式）</li>
<li>同步设备并释放输出内存</li>
</ol>
<h3 id="42-load-方法实现">4.2 load 方法实现</h3>
<pre><code class="language-cpp">void Graph::load(std::shared_ptr&lt;InputFile&gt; fin, LlmParams&amp; param, std::shared_ptr&lt;Vocab&gt; vocab) {
    // 验证魔数
    uint32_t magic;
    fin-&gt;read_raw((char*)&amp;magic, sizeof(magic));
    INFER_ASSERT(magic == 0x123456, &quot;model magic is not create!!!!&quot;);
    
    // 加载参数
    load_param(fin, param, vocab);
    
    // 构建模型结构
    construct_llm();
    collect_weights();
    set_weights_alias();
    
    // 加载权重
    while (true) {
        // 读取权重信息
        int32_t n_dims, length, ftype;
        if (fin-&gt;eof()) break;
        
        fin-&gt;read_raw(reinterpret_cast&lt;char*&gt;(&amp;n_dims), sizeof(n_dims));
        fin-&gt;read_raw(reinterpret_cast&lt;char*&gt;(&amp;length), sizeof(length));
        fin-&gt;read_raw(reinterpret_cast&lt;char*&gt;(&amp;ftype), sizeof(ftype));
        
        if (fin-&gt;eof()) break;
        
        // 读取形状
        size_t nr_number = 1;
        int32_t shape[2] = {1, 1};
        for (int i = 0; i &lt; n_dims; ++i) {
            fin-&gt;read_raw(reinterpret_cast&lt;char*&gt;(&amp;shape[i]), sizeof(shape[i]));
            nr_number *= shape[i];
        }
        
        // 读取权重名称
        std::string name(length, 0);
        fin-&gt;read_raw(&amp;name[0], length);
        auto alias_name = get_weight_alias(name);
        
        // 如果权重不在映射表中，跳过
        if (m_weights_map.count(alias_name) == 0) {
            auto dtype = convert_dtype(ftype);
            size_t length = nr_number * dtype_in_byte(dtype) / dtype_block_size(dtype);
            fin-&gt;skip(length);
            continue;
        }
        
        // 设置权重的文件信息
        auto weight = m_weights_map[alias_name];
        weight-&gt;set_file(fin, fin-&gt;tell());
        weight-&gt;set_shape({shape[0], shape[1]}, convert_dtype(ftype));

        // 跳过权重数据（实际读取在需要时进行）
        size_t length = weight-&gt;length_in_byte();
        fin-&gt;skip(length);
</code></pre>
<p>这里采用了延迟加载策略，只记录权重在文件中的位置，实际读取在需要时进行，这样可以减少内存使用。</p>
<h3 id="43-collect_weights-方法">4.3 collect_weights 方法</h3>
<pre><code class="language-cpp">void Graph::collect_weights() {
    for (auto module : m_modules) {
        auto weights = module-&gt;get_all_weights();
        for (auto weight : weights) {
            auto name = weight-&gt;name();
            m_weights_map[name] = weight;
        }
    }
}
</code></pre>
<p>这个方法收集所有模块的权重，并将它们存储在权重映射表中，以便后续加载。</p>
<h3 id="44-reset_ctx-方法">4.4 reset_ctx 方法</h3>
<pre><code class="language-cpp">void Graph::reset_ctx() {
    for (auto module : m_modules) {
        module-&gt;reset_ctx();
    }
}
</code></pre>
<p>这个方法重置所有模块的上下文，通常在处理新的序列时调用。</p>
<h2 id="5-模型构建流程">5. 模型构建流程</h2>
<p>InferLLM 框架中的模型构建流程如下：</p>
<ol>
<li>
<p><strong>创建 Graph 对象</strong>：</p>
<pre><code class="language-cpp">auto graph = Graph::make_graph(model_config, device, name);
</code></pre>
</li>
<li>
<p><strong>加载模型参数</strong>：</p>
<pre><code class="language-cpp">graph-&gt;load(fin, param, vocab);
</code></pre>
<p>这会调用 <code>load_param</code> 加载模型参数，然后调用 <code>construct_llm</code> 构建模型结构。</p>
</li>
<li>
<p><strong>构建模型结构</strong>：<br>
<code>construct_llm</code> 方法由派生类实现，用于构建特定模型的结构。例如，LLaMA 模型的构建过程会包含以下步骤：</p>
<ul>
<li>创建输入张量</li>
<li>创建嵌入模块</li>
<li>创建多个 Transformer 层，每层包含：
<ul>
<li>层归一化</li>
<li>注意力模块</li>
<li>残差连接</li>
<li>层归一化</li>
<li>FFN 模块</li>
<li>残差连接</li>
</ul>
</li>
<li>创建输出头模块</li>
</ul>
</li>
<li>
<p><strong>收集权重</strong>：</p>
<pre><code class="language-cpp">graph-&gt;collect_weights();
</code></pre>
<p>这会收集所有模块的权重，并将它们存储在权重映射表中。</p>
</li>
<li>
<p><strong>设置权重别名</strong>：</p>
<pre><code class="language-cpp">graph-&gt;set_weights_alias();
</code></pre>
<p>这会设置权重的别名，用于处理不同模型格式的权重名称差异。</p>
</li>
</ol>
<h2 id="6-模型执行流程">6. 模型执行流程</h2>
<p>InferLLM 框架中的模型执行流程如下：</p>
<ol>
<li>
<p><strong>准备输入</strong>：</p>
<pre><code class="language-cpp">graph-&gt;execute(tokens, logits, nr_past, prefill);
</code></pre>
<p>这会将输入 token 复制到设备内存，并准备工作空间。</p>
</li>
<li>
<p><strong>执行模块</strong>：</p>
<pre><code class="language-cpp">for (size_t i = 0; i &lt; m_modules.size(); i++) {
    m_modules[i]-&gt;execute(m_workspace.get(), nr_past, prefill);
}
</code></pre>
<p>这会按顺序执行所有模块。</p>
</li>
<li>
<p><strong>获取输出</strong>：</p>
<pre><code class="language-cpp">m_device-&gt;device2host_copy(logist.data(), m_output-&gt;ptr(), logist.size() * sizeof(float), true);
</code></pre>
<p>这会将输出 logits 复制到主机内存。</p>
</li>
<li>
<p><strong>同步和清理</strong>：</p>
<pre><code class="language-cpp">m_device-&gt;sync();
m_output-&gt;recall_data();
</code></pre>
<p>这会等待所有操作完成，并释放输出内存。</p>
</li>
</ol>
<h2 id="7-优化策略">7. 优化策略</h2>
<p>Graph 类实现了几种优化策略：</p>
<ol>
<li><strong>延迟加载</strong>：权重数据只在需要时才从文件读取，减少内存使用</li>
<li><strong>工作空间复用</strong>：使用同一块内存作为所有算子的工作空间，减少内存分配</li>
<li><strong>预填充模式</strong>：支持预填充模式，可以跳过输出头的计算，提高性能</li>
<li><strong>上下文重置</strong>：支持重置上下文，便于处理新的序列</li>
</ol>
<h2 id="8-tensor-类与-graph-类的交互">8. Tensor 类与 Graph 类的交互</h2>
<p>Tensor 类是 Graph 类的基础，它提供了数据存储和管理功能。Graph 类通过以下方式与 Tensor 类交互：</p>
<ol>
<li><strong>创建张量</strong>：Graph 类创建输入、输出和中间张量</li>
<li><strong>管理权重</strong>：Graph 类管理模型权重，这些权重是特殊的张量</li>
<li><strong>数据传输</strong>：Graph 类负责在主机和设备之间传输数据</li>
<li><strong>内存管理</strong>：Graph 类通过 Tensor 类的 <code>prepare_data</code> 和 <code>recall_data</code> 方法管理内存</li>
</ol>
<h2 id="总结">总结</h2>
<p>Graph 类是 InferLLM 框架的核心组件，它通过模块化的设计，实现了灵活、高效的模型构建和执行。它支持多种模型架构（如 LLaMA、ChatGLM 等），并提供了统一的接口进行模型加载和推理。通过延迟加载、工作空间复用等优化策略，它可以在有限的内存资源下高效运行大型语言模型。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(05)——Device类的实现(src/core/device.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-05/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-05/">
        </link>
        <updated>2025-03-25T06:47:28.000Z</updated>
        <content type="html"><![CDATA[<h1 id="device-类代码结构与功能实现分析">Device 类代码结构与功能实现分析</h1>
<p><code>Device</code> 类是 InferLLM 框架中的设备抽象层，负责内存管理和数据传输。它提供了统一的接口，使上层代码可以在不同硬件平台（CPU、GPU）上运行而无需修改。</p>
<h2 id="1-基类-device">1. 基类 Device</h2>
<h3 id="11-核心接口设计">1.1 核心接口设计</h3>
<p><code>Device</code> 是一个抽象基类，定义了以下核心接口：</p>
<pre><code class="language-cpp">class Device {
public:
    // 内存分配与释放
    virtual void* allocate(size_t len) = 0;
    virtual void* allocate_host(size_t len) = 0;
    virtual void free_device(void* ptr) = 0;
    virtual void free_host(void* ptr) = 0;
    
    // 数据传输
    virtual void host2device_copy(void* device, const void* host, size_t size, bool async = false) = 0;
    virtual void device2host_copy(void* host, const void* device, size_t size, bool async = false) = 0;
    virtual void device2device_copy(void* dst, const void* src, size_t size, bool async = false) = 0;
    
    // 同步操作
    virtual void sync() = 0;
    
    // 内存对齐分配
    virtual void* aligned_alloc(size_t size);
    virtual void aligned_free(void* ptr);
    
    // 设备特性查询
    virtual bool unified_memory() { return true; }
    
    // 内核访问
    Kernel* kernel() { return m_kernel.get(); }
    KernelType type() { return m_kernel-&gt;m_kernel_type; };
};
</code></pre>
<h3 id="12-内存对齐实现">1.2 内存对齐实现</h3>
<p><code>Device</code> 类提供了跨平台的内存对齐分配实现：</p>
<pre><code class="language-cpp">void* Device::aligned_alloc(size_t size) {
#ifdef WIN32
    return _aligned_malloc(size, ALIGN_SIZE);
#elif defined(__ANDROID__) || defined(ANDROID)
    return memalign(ALIGN_SIZE, size);
#else
    void* ptr = nullptr;
    auto err = posix_memalign(&amp;ptr, ALIGN_SIZE, size);
    INFER_ASSERT(!err, &quot;failed to malloc.&quot;);
    return ptr;
#endif
}

void Device::aligned_free(void* ptr) {
#ifdef WIN32
    _aligned_free(ptr);
#else
    ::free(ptr);
#endif
}
</code></pre>
<p>这里使用了条件编译，根据不同平台选择合适的内存对齐函数。对齐大小固定为 32 字节，这有利于 SIMD 指令的优化。</p>
<h3 id="13-内存池管理">1.3 内存池管理</h3>
<p><code>Device</code> 类使用两个映射表实现简单的内存池：</p>
<pre><code class="language-cpp">protected:
    std::map&lt;void*, size_t&gt; m_alloc_memory;  // 已分配内存的大小记录
    std::map&lt;size_t, std::vector&lt;void*&gt;&gt; m_free_memory;  // 按大小分类的空闲内存块
</code></pre>
<h2 id="2-cpudevice-实现">2. CPUDevice 实现</h2>
<p><code>CPUDevice</code> 是 <code>Device</code> 的 CPU 实现，它使用线程池进行并行计算。</p>
<h3 id="21-构造与初始化">2.1 构造与初始化</h3>
<pre><code class="language-cpp">CPUDevice(KernelType type, uint32_t nr_thread) : Device() {
    m_thread_pool = make_unique&lt;ThreadPool&gt;(nr_thread);
    m_kernel = make_unique&lt;Kernel&gt;(type, m_thread_pool.get());
}
</code></pre>
<p>构造函数创建了线程池和对应类型的计算内核。</p>
<h3 id="22-内存分配与回收">2.2 内存分配与回收</h3>
<pre><code class="language-cpp">void* CPUDevice::allocate(size_t len) {
#ifdef ENABLE_ASAN
    return aligned_alloc(len);
#else
    auto it = m_free_memory.lower_bound(len);
    void* ptr = nullptr;
    if (it != m_free_memory.end() &amp;&amp; it-&gt;second.size() &gt; 0) {
        ptr = it-&gt;second.back();
        it-&gt;second.pop_back();
        if (it-&gt;second.size() &lt; 1) {
            m_free_memory.erase(it);
        }
    } else {
        ptr = aligned_alloc(len);
        m_alloc_memory[ptr] = len;
    }
    return ptr;
#endif
}
</code></pre>
<p><code>allocate</code> 方法实现了内存池复用：</p>
<ol>
<li>首先查找大小大于等于请求大小的最小空闲块</li>
<li>如果找到，则复用该块</li>
<li>否则，分配新的内存块并记录</li>
</ol>
<pre><code class="language-cpp">void CPUDevice::free_device(void* ptr) {
#ifdef ENABLE_ASAN
    aligned_free(ptr);
#else
    INFER_ASSERT(
            m_alloc_memory.count(ptr) == 1,
            &quot;memory is not allocated by the DeviceCPU.&quot;);
    size_t len = m_alloc_memory[ptr];
    m_free_memory[len].push_back(ptr);
#endif
}
</code></pre>
<p><code>free_device</code> 方法不会真正释放内存，而是将其放回内存池中以便复用。</p>
<h3 id="23-数据传输">2.3 数据传输</h3>
<p>由于 CPU 是统一内存架构，数据传输操作只是简单的内存拷贝：</p>
<pre><code class="language-cpp">void host2device_copy(void* device, const void* host, size_t size, bool async = false) override {
    memcpy(device, host, size);
}

void device2host_copy(void* host, const void* device, size_t size, bool async = false) override {
    memcpy(host, device, size);
}

void device2device_copy(void* dst, const void* src, size_t size, bool async = false) override {
    memcpy(dst, src, size);
}
</code></pre>
<h3 id="24-析构函数">2.4 析构函数</h3>
<pre><code class="language-cpp">CPUDevice::~CPUDevice() {
#ifndef ENABLE_ASAN
    for (auto it : m_free_memory) {
        for (auto ptr : it.second) {
            INFER_ASSERT(
                    m_alloc_memory.count(ptr) == 1,
                    &quot;memory is not allocated by the DeviceCPU.&quot;);
            aligned_free(ptr);
        }
    }
#endif
}
</code></pre>
<p>析构函数负责释放内存池中的所有内存块。</p>
<h2 id="3-gpudevice-实现">3. GPUDevice 实现</h2>
<p><code>GPUDevice</code> 是 <code>Device</code> 的 GPU 实现，它使用 CUDA 进行内存管理和计算。这部分代码通过条件编译 <code>#if ENABLE_GPU</code> 控制。</p>
<h3 id="31-构造与初始化">3.1 构造与初始化</h3>
<pre><code class="language-cpp">GPUDevice(int device) : Device() {
    CUDA_CHECK(cudaSetDevice(0));
    CUDA_CHECK(cudaStreamCreate(&amp;(m_handle.stream)));
    CUBLAS_CHECK(cublasCreate(&amp;(m_handle.cublas_handle)));
    m_kernel = make_unique&lt;Kernel&gt;(KernelType::GPU);
    m_kernel-&gt;set_handle(&amp;m_handle);
}
</code></pre>
<p>构造函数初始化 CUDA 环境，创建 CUDA 流和 cuBLAS 句柄。</p>
<h3 id="32-内存分配与回收">3.2 内存分配与回收</h3>
<pre><code class="language-cpp">void* GPUDevice::allocate(size_t len) {
    auto it = m_free_memory.lower_bound(len);
    void* ptr = nullptr;

    if (it != m_free_memory.end() &amp;&amp; it-&gt;second.size() &gt; 0) {
        ptr = it-&gt;second.back();
        it-&gt;second.pop_back();
        if (it-&gt;second.size() &lt; 1) {
            m_free_memory.erase(it);
        }
    } else {
        CUDA_CHECK(cudaMalloc(&amp;ptr, len));
        m_alloc_memory[ptr] = len;
    }
    return ptr;
}
</code></pre>
<p>与 <code>CPUDevice</code> 类似，<code>GPUDevice</code> 也实现了内存池复用，但使用 <code>cudaMalloc</code> 分配 GPU 内存。</p>
<pre><code class="language-cpp">void* GPUDevice::allocate_host(size_t len) {
    void* ptr = nullptr;
    CUDA_CHECK(cudaMallocHost(&amp;ptr, len));
    return ptr;
}
</code></pre>
<p><code>allocate_host</code> 使用 <code>cudaMallocHost</code> 分配可锁页（pinned）内存，这种内存可以更高效地与 GPU 进行数据传输。</p>
<h3 id="33-数据传输">3.3 数据传输</h3>
<pre><code class="language-cpp">void GPUDevice::host2device_copy(void* device, const void* host, size_t size, bool async) {
    if (async) {
        CUDA_CHECK(cudaMemcpyAsync(
                device, host, size, cudaMemcpyHostToDevice, m_handle.stream));
    } else {
        CUDA_CHECK(cudaMemcpy(device, host, size, cudaMemcpyHostToDevice));
    }
}
</code></pre>
<p>数据传输方法使用 CUDA 内存拷贝函数，支持同步和异步两种模式。异步模式使用 CUDA 流进行操作，可以与计算重叠。</p>
<h3 id="34-同步操作">3.4 同步操作</h3>
<pre><code class="language-cpp">void sync() override { CUDA_CHECK(cudaStreamSynchronize(m_handle.stream)); }
</code></pre>
<p><code>sync</code> 方法等待 CUDA 流中的所有操作完成。</p>
<h3 id="35-内存特性">3.5 内存特性</h3>
<pre><code class="language-cpp">bool unified_memory() override { return false; }
</code></pre>
<p>GPU 不是统一内存架构，因此 <code>unified_memory</code> 返回 <code>false</code>。</p>
<h3 id="36-析构函数">3.6 析构函数</h3>
<pre><code class="language-cpp">GPUDevice::~GPUDevice() {
    for (auto it : m_free_memory) {
        for (auto ptr : it.second) {
            INFER_ASSERT(
                    m_alloc_memory.count(ptr) == 1,
                    &quot;memory is not allocated by the DeviceCPU.&quot;);
            CUDA_CHECK(cudaFree(ptr));
        }
    }
    CUDA_CHECK(cudaStreamDestroy(m_handle.stream));
    CUBLAS_CHECK(cublasDestroy(m_handle.cublas_handle));
}
</code></pre>
<p>析构函数释放所有 GPU 内存，并销毁 CUDA 流和 cuBLAS 句柄。</p>
<h2 id="4-内存管理策略">4. 内存管理策略</h2>
<p>Device 类实现了几种内存管理策略：</p>
<ol>
<li><strong>内存池复用</strong>：通过 <code>m_free_memory</code> 和 <code>m_alloc_memory</code> 实现内存块的复用，减少内存分配和释放的开销</li>
<li><strong>内存对齐</strong>：使用对齐内存分配，提高内存访问效率，特别是对 SIMD 指令</li>
<li><strong>可锁页内存</strong>：在 GPU 实现中使用可锁页内存，提高主机和设备之间的数据传输效率</li>
<li><strong>异步操作</strong>：支持异步数据传输，可以与计算重叠，提高整体性能</li>
</ol>
<h2 id="5-设备抽象的意义">5. 设备抽象的意义</h2>
<p>Device 类的抽象设计有以下几个重要意义：</p>
<ol>
<li><strong>硬件无关性</strong>：上层代码可以不关心具体硬件细节，通过统一接口操作不同设备</li>
<li><strong>代码复用</strong>：共同的内存管理逻辑可以在基类中实现，派生类只需关注特定设备的实现</li>
<li><strong>扩展性</strong>：可以方便地添加新的设备支持，如 TPU、NPU 等</li>
<li><strong>优化空间</strong>：可以在不改变上层代码的情况下，优化特定设备的内存管理和数据传输</li>
</ol>
<h2 id="总结">总结</h2>
<p>Device 类是 InferLLM 框架中的关键组件，它提供了统一的设备抽象，封装了内存管理和数据传输的细节。通过内存池、内存对齐等优化策略，提高了内存使用效率。同时，它的设计使得框架可以在不同硬件平台上高效运行，为上层提供了一个稳定、高效的基础设施。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(04)——Tensor类的实现(src/core/tensor.h+.cpp)]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-04/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-04/">
        </link>
        <updated>2025-03-22T06:37:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="tensor-类代码结构与功能实现分析">Tensor 类代码结构与功能实现分析</h1>
<p><code>Tensor</code> 类是 InferLLM 框架中的核心数据结构，用于表示和管理多维数组数据。下面对其代码结构和功能实现进行详细分析。</p>
<h2 id="1-数据类型定义">1. 数据类型定义</h2>
<p>首先，框架定义了多种数据类型，以支持不同精度的计算：</p>
<pre><code class="language-cpp">enum class DType {
    Float32 = 0,
    Float16 = 1,
    Float8 = 2,
    Int32 = 3,
    Int16 = 4,
    Int8 = 5,
    Uint8 = 6,
    Int4 = 7,
    Uint4 = 8,
    Int2 = 9,
};
</code></pre>
<p>同时定义了两个辅助函数：</p>
<ul>
<li><code>dtype_in_byte</code>：获取数据类型的字节大小</li>
<li><code>dtype_block_size</code>：获取数据类型的块大小（用于量化类型）</li>
</ul>
<h2 id="2-tensor-类的内存管理">2. Tensor 类的内存管理</h2>
<p>Tensor 类支持三种内存来源方式：</p>
<ol>
<li>自己分配的内存</li>
<li>外部共享的内存</li>
<li>从文件映射的内存（主要用于权重）</li>
</ol>
<p>这通过 <code>TensorState</code> 枚举进行管理：</p>
<pre><code class="language-cpp">enum class TensorState {
    Own = 0,    // 拥有内存
    OutSide = 1, // 内存在外部
};
</code></pre>
<h2 id="3-tensor-类的主要成员变量">3. Tensor 类的主要成员变量</h2>
<pre><code class="language-cpp">private:
    bool m_shared = false;           // 是否是共享内存
    int32_t m_usr_count = 0;         // 用户计数（引用计数）
    int32_t m_cur_count = 0;         // 当前用户计数
    
    Device* m_device;                // 设备指针
    OpBase* m_owner_op;              // 拥有该张量的算子
    
    TensorState m_state;             // 张量状态
    std::shared_ptr&lt;InputFile&gt; m_file; // 文件指针（用于从文件读取数据）
    size_t m_file_offset = 0;        // 文件偏移
    
    uint32_t m_dims = 0;             // 维度数
    size_t m_length = 0;             // 元素总数
    DType m_dtype;                   // 数据类型
    std::vector&lt;size_t&gt; m_shape;     // 形状
    std::vector&lt;size_t&gt; m_stride;    // 步长
    void* m_data = nullptr;          // 数据指针
    std::string m_name;              // 名称
</code></pre>
<h2 id="4-主要功能实现">4. 主要功能实现</h2>
<h3 id="41-构造函数">4.1 构造函数</h3>
<p>Tensor 类提供了两种构造方式：</p>
<pre><code class="language-cpp">// 通过设备和名称构造
Tensor(Device* device, std::string name);

// 通过形状、数据类型和设备构造
Tensor(std::vector&lt;size_t&gt; shape, DType dtype, Device* device);
</code></pre>
<p>初始状态都是 <code>TensorState::OutSide</code>，表示数据不在设备上。</p>
<h3 id="42-形状和数据类型管理">4.2 形状和数据类型管理</h3>
<pre><code class="language-cpp">// 设置形状
void set_shape(std::vector&lt;size_t&gt; shape);

// 计算步长和总长度
m_stride.resize(m_dims);
m_stride[m_dims - 1] = 1;
for (uint32_t i = 1; i &lt; m_dims; i++) {
    m_stride[m_dims - 1 - i] = m_stride[m_dims - i] * m_shape[m_dims - i];
}
m_length = m_shape[0] * m_stride[0];
</code></pre>
<p>这里的步长计算采用了行优先（row-major）的存储方式，与 C/C++ 的多维数组存储一致。</p>
<h3 id="43-内存管理核心方法">4.3 内存管理核心方法</h3>
<h4 id="prepare_data">prepare_data()</h4>
<pre><code class="language-cpp">TensorState Tensor::prepare_data() {
    size_t length = length_in_byte();
    if (!m_data &amp;&amp; m_state == TensorState::OutSide) {
        if (m_file) {
            read_data_from_file();
        } else {
            m_data = m_device-&gt;allocate(length);
        }
    }
    m_state = TensorState::Own;
    return m_state;
}
</code></pre>
<p>该方法确保张量数据在设备上可用：</p>
<ul>
<li>如果数据不存在且状态为 <code>OutSide</code>
<ul>
<li>如果有关联文件，从文件读取</li>
<li>否则，从设备分配内存</li>
</ul>
</li>
<li>将状态设置为 <code>Own</code></li>
</ul>
<h4 id="recall_data">recall_data()</h4>
<pre><code class="language-cpp">TensorState Tensor::recall_data() {
    if (m_shared) {
        return m_state;
    }
    if (!m_file &amp;&amp; m_data != nullptr &amp;&amp; m_state == TensorState::Own) {
        m_device-&gt;free_device(m_data);
        m_data = nullptr;
    }
    m_state = TensorState::OutSide;
    return m_state;
}
</code></pre>
<p>该方法释放张量占用的设备内存：</p>
<ul>
<li>如果是共享内存，不做任何操作</li>
<li>如果不是从文件读取的，且数据存在且状态为 <code>Own</code>，释放设备内存</li>
<li>将状态设置为 <code>OutSide</code></li>
</ul>
<h3 id="44-文件数据读取">4.4 文件数据读取</h3>
<pre><code class="language-cpp">size_t Tensor::read_data_from_file() {
    size_t length = length_in_byte();
    if (m_file-&gt;enable_mmap()) {
        // 使用内存映射
        if (!m_device-&gt;unified_memory()) {
            // 非统一内存设备（如GPU）
            auto temp_ptr = m_file-&gt;get_mmap_data(length, m_file_offset);
            m_data = m_device-&gt;allocate(length);
            m_device-&gt;host2device_copy(m_data, temp_ptr, length);
        } else {
            // 统一内存设备（如CPU）
            m_data = m_file-&gt;get_mmap_data(length, m_file_offset);
        }
    } else {
        // 不使用内存映射，直接读取
        // ...（处理统一内存和非统一内存的情况）
    }
    return length;
}
</code></pre>
<p>该方法从文件读取数据到张量：</p>
<ul>
<li>支持内存映射和直接读取两种方式</li>
<li>针对统一内存设备（CPU）和非统一内存设备（GPU）有不同处理</li>
<li>支持权重预处理</li>
</ul>
<h3 id="45-引用计数管理">4.5 引用计数管理</h3>
<pre><code class="language-cpp">int32_t add_user() {
    m_usr_count++;
    return m_usr_count;
}

int32_t decrease_curr_user_count() {
    if (!m_shared) {
        INFER_ASSERT(m_cur_count &gt; 0, &quot;The user count is less than 0.&quot;);
        m_cur_count--;
        if (m_cur_count == 0) {
            recall_data();
        }
    }
    return m_cur_count;
}

int32_t resume_user_count() {
    m_cur_count = m_usr_count;
    return m_cur_count;
}
</code></pre>
<p>这组方法实现了引用计数机制：</p>
<ul>
<li><code>add_user()</code>：增加用户计数</li>
<li><code>decrease_curr_user_count()</code>：减少当前用户计数，当计数为0时释放内存</li>
<li><code>resume_user_count()</code>：恢复用户计数（用于重用张量）</li>
</ul>
<h3 id="46-数据访问">4.6 数据访问</h3>
<pre><code class="language-cpp">void* ptr() {
    INFER_ASSERT(is_own(), &quot;Tensor is OutSide the device, can't get the memory.&quot;);
    return m_data;
}

template &lt;typename T&gt;
T* ptr() {
    INFER_ASSERT(is_own(), &quot;Tensor is OutSide the device, can't get the memory.&quot;);
    return static_cast&lt;T*&gt;(m_data);
}
</code></pre>
<p>这些方法提供了对张量数据的访问：</p>
<ul>
<li>确保数据在设备上可用</li>
<li>支持类型转换</li>
</ul>
<h2 id="5-workspace-类">5. WorkSpace 类</h2>
<p><code>WorkSpace</code> 是一个简单的内存容器类，用于临时存储计算过程中的数据：</p>
<pre><code class="language-cpp">class WorkSpace {
public:
    void* ptr() { return m_data; };
    
    template &lt;typename T&gt;
    T* ptr() {
        return static_cast&lt;T*&gt;(m_data);
    }
    
    size_t length() { return m_length; }
    
    void set_memory(void* data, size_t length) {
        m_data = data;
        m_length = length;
    }
    
private:
    void* m_data = nullptr;
    size_t m_length = 0;
};
</code></pre>
<p>它不负责内存的分配和释放，只是提供一个访问接口。</p>
<h2 id="6-内存优化策略">6. 内存优化策略</h2>
<p>Tensor 类实现了几种内存优化策略：</p>
<ol>
<li><strong>延迟分配</strong>：只有在实际需要时才分配内存</li>
<li><strong>引用计数</strong>：通过引用计数管理内存生命周期</li>
<li><strong>内存映射</strong>：支持从文件直接映射内存，减少内存拷贝</li>
<li><strong>内存共享</strong>：支持共享外部内存，避免不必要的拷贝</li>
</ol>
<h2 id="总结">总结</h2>
<p>Tensor类是 InferLLM 框架的核心数据结构，提供了灵活的内存管理和数据访问机制。它支持多种数据类型、多种内存来源方式，并通过引用计数优化内存使用。同时，它还提供了从文件读取数据和数据预处理的功能，为模型权重的加载和处理提供了支持。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(03)——InferLLM核心模块core(src/core)综合介绍]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-03/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-03/">
        </link>
        <updated>2025-03-16T06:31:58.000Z</updated>
        <content type="html"><![CDATA[<h1 id="inferllm核心模块coresrccore结构与功能">InferLLM核心模块core(src/core)结构与功能</h1>
<p>InferLLM 的 core(src/core) 模块包含了框架的核心组件，负责模型的基础数据结构、内存管理、计算图构建和执行等功能。下面对 core 模块下的主要文件进行分析：</p>
<h2 id="1-核心数据结构">1. 核心数据结构</h2>
<h3 id="11-tensor-tensorhcpp">1.1 Tensor (tensor.h/cpp)</h3>
<p><code>Tensor</code> 是框架的基础数据结构，用于表示多维数组数据：</p>
<ul>
<li><strong>内存管理</strong>：支持三种内存来源
<ul>
<li>自己分配的内存</li>
<li>外部共享的内存</li>
<li>从文件映射的内存（权重）</li>
</ul>
</li>
<li><strong>数据类型</strong>：支持多种数据类型（Float32, Int4, Int8等）</li>
<li><strong>内存状态</strong>：通过 <code>TensorState</code> 管理（Own, OutSide）</li>
<li><strong>用户计数</strong>：通过引用计数管理内存生命周期</li>
<li><strong>数据预处理</strong>：支持权重数据的预处理</li>
</ul>
<p>关键方法：</p>
<pre><code class="language-cpp">TensorState prepare_data();  // 准备数据，如果数据不存在则分配或从文件读取
TensorState recall_data();   // 回收数据，释放不需要的内存
void* ptr();                 // 获取数据指针
</code></pre>
<h3 id="12-workspace-tensorh">1.2 WorkSpace (tensor.h)</h3>
<p><code>WorkSpace</code> 是一个简单的内存容器，用于临时存储计算过程中的数据：</p>
<pre><code class="language-cpp">class WorkSpace {
public:
    void* ptr();
    template &lt;typename T&gt; T* ptr();
    size_t length();
    void set_memory(void* data, size_t length);
private:
    void* m_data = nullptr;
    size_t m_length = 0;
};
</code></pre>
<h2 id="2-设备抽象">2. 设备抽象</h2>
<h3 id="21-device-devicehcpp">2.1 Device (device.h/cpp)</h3>
<p><code>Device</code> 是对计算设备的抽象，提供内存分配和数据传输功能：</p>
<ul>
<li><strong>内存管理</strong>：分配和释放设备内存</li>
<li><strong>数据传输</strong>：在主机和设备之间传输数据</li>
<li><strong>内核管理</strong>：持有对应设备的计算内核</li>
</ul>
<p>派生类：</p>
<ul>
<li><code>CPUDevice</code>：CPU设备实现</li>
<li><code>GPUDevice</code>：GPU设备实现（条件编译）</li>
</ul>
<p>关键方法：</p>
<pre><code class="language-cpp">virtual void* allocate(size_t len) = 0;
virtual void free_device(void* ptr) = 0;
virtual void host2device_copy(void* device, const void* host, size_t size, bool async = false) = 0;
</code></pre>
<h2 id="3-计算图构建">3. 计算图构建</h2>
<h3 id="31-graph-graphhcpp">3.1 Graph (graph.h/cpp)</h3>
<p><code>Graph</code> 是模型的骨架，定义了计算流程和模型结构：</p>
<ul>
<li><strong>模块管理</strong>：管理所有计算模块</li>
<li><strong>权重管理</strong>：管理模型权重</li>
<li><strong>执行流程</strong>：控制模型的前向计算</li>
<li><strong>工作空间</strong>：管理计算过程中的临时内存</li>
</ul>
<p>关键方法：</p>
<pre><code class="language-cpp">void execute(std::vector&lt;int32_t&gt; in_token, std::vector&lt;float&gt;&amp; logist, uint32_t nr_past, bool prefill = false);
virtual void construct_llm() = 0;  // 构建LLM模型结构，由派生类实现
</code></pre>
<h3 id="32-oprmodulebase-graphh">3.2 OprModuleBase (graph.h)</h3>
<p><code>OprModuleBase</code> 是计算图中的基本单元，封装了一组相关的算子：</p>
<ul>
<li><strong>算子管理</strong>：管理一组相关的算子</li>
<li><strong>输入输出</strong>：管理模块的输入输出张量</li>
<li><strong>执行控制</strong>：控制模块的执行</li>
</ul>
<p>派生类：</p>
<ul>
<li><code>AttentionModule</code>：注意力模块</li>
<li><code>LlamaFFNModule</code>：LLaMA前馈网络模块</li>
<li><code>GlmFFNModule</code>：GLM前馈网络模块</li>
<li><code>HeadModule</code>：输出头模块</li>
<li><code>EmbdModule</code>：嵌入模块</li>
</ul>
<h2 id="4-键值存储">4. 键值存储</h2>
<h3 id="41-kvstorage-kvstoragehcpp">4.1 KvStorage (kvstorage.h/cpp)</h3>
<p><code>KvStorage</code> 继承自 <code>Tensor</code>，专门用于存储和管理键值对：</p>
<ul>
<li><strong>索引管理</strong>：管理存储的键值对索引</li>
<li><strong>内存管理</strong>：管理键值对的内存</li>
<li><strong>数据访问</strong>：提供对当前键值对的访问</li>
</ul>
<p>关键方法：</p>
<pre><code class="language-cpp">void* get_current_data();  // 获取当前数据指针
size_t add_id(uint32_t id);  // 添加索引
void reset_id();  // 重置索引
</code></pre>
<h2 id="5-模型接口">5. 模型接口</h2>
<h3 id="51-model-modelcpp">5.1 Model (model.cpp)</h3>
<p><code>Model</code> 提供了对外的模型接口，封装了内部实现：</p>
<ul>
<li><strong>解码接口</strong>：提供文本解码功能</li>
<li><strong>模型管理</strong>：管理模型的生命周期</li>
</ul>
<p>关键方法：</p>
<pre><code class="language-cpp">std::string decode(const std::string&amp; user_input, int&amp; token);
std::string decode_iter(int&amp; token);
</code></pre>
<h2 id="6-线程池">6. 线程池</h2>
<h3 id="61-threadpool-thread_poolhcpp">6.1 ThreadPool (thread_pool.h/cpp)</h3>
<p><code>ThreadPool</code> 提供了线程管理和任务调度功能：</p>
<ul>
<li><strong>线程管理</strong>：创建和管理工作线程</li>
<li><strong>任务调度</strong>：分配任务给工作线程</li>
<li><strong>同步机制</strong>：提供任务同步机制</li>
</ul>
<h2 id="7-核心功能流程">7. 核心功能流程</h2>
<ol>
<li>
<p><strong>模型加载</strong>：</p>
<ul>
<li>创建 <code>Graph</code> 对象</li>
<li>构建模型结构 (<code>construct_llm</code>)</li>
<li>加载模型权重</li>
</ul>
</li>
<li>
<p><strong>推理执行</strong>：</p>
<ul>
<li>准备输入数据</li>
<li>执行计算图 (<code>Graph::execute</code>)</li>
<li>模块按顺序执行 (<code>OprModuleBase::execute</code>)</li>
<li>获取输出结果</li>
</ul>
</li>
<li>
<p><strong>内存管理</strong>：</p>
<ul>
<li>按需分配内存 (<code>Tensor::prepare_data</code>)</li>
<li>使用完毕释放内存 (<code>Tensor::recall_data</code>)</li>
<li>通过引用计数管理内存生命周期</li>
</ul>
</li>
</ol>
<h2 id="总结">总结</h2>
<p>InferLLM 的 core 模块提供了一个灵活、高效的框架，用于构建和执行 LLM 模型。它通过抽象设备、张量、计算图等概念，实现了对不同硬件平台的支持和高效的内存管理。核心模块的设计清晰，各组件职责明确，便于扩展和维护。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(02)——InferLLM的src模块]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-02/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-02/">
        </link>
        <updated>2025-03-14T10:41:06.000Z</updated>
        <content type="html"><![CDATA[<p>接下来来详细介绍<a href="https://github.com/MegEngine/InferLLM">InferLLM</a>项目 src 模块的代码结构和功能：</p>
<h3 id="1-核心组件-core">1. 核心组件 (<code>core/</code>)</h3>
<ol>
<li><strong>设备和内存管理</strong></li>
</ol>
<ul>
<li><strong>name=&quot;device.h/cpp&quot; path=&quot;src/core/device.h&quot;</strong>: 设备抽象层，管理计算设备(CPU/GPU)和内存分配</li>
<li><strong>name=&quot;tensor.h/cpp&quot; path=&quot;src/core/tensor.h&quot;</strong>: 张量数据结构，支持多种数据类型和设备</li>
</ul>
<ol>
<li><strong>计算图和模型</strong></li>
</ol>
<ul>
<li><strong>name=&quot;graph.h/cpp&quot; path=&quot;src/core/graph.h&quot;</strong>: 计算图的基础定义和管理</li>
<li><strong>name=&quot;model.cpp&quot; path=&quot;src/core/model.cpp&quot;</strong>: 模型加载和管理</li>
<li><strong>name=&quot;model_imp.h/cpp&quot; path=&quot;src/core/model_imp.h&quot;</strong>: 模型实现的具体细节</li>
</ul>
<ol>
<li><strong>算子系统</strong></li>
</ol>
<ul>
<li><strong>name=&quot;op.h/cpp&quot; path=&quot;src/core/op.h&quot;</strong>: 基础算子定义，包括：
<ul>
<li>Embedding</li>
<li>LayerNorm</li>
<li>MatMul</li>
<li>Elemwise 等基础算子</li>
</ul>
</li>
</ul>
<ol>
<li><strong>其他核心功能</strong></li>
</ol>
<ul>
<li><strong>name=&quot;kvstorage.h&quot; path=&quot;src/core/kvstorage.h&quot;</strong>: KV缓存管理</li>
<li><strong>name=&quot;thread_pool.h/cpp&quot; path=&quot;src/core/thread_pool.h&quot;</strong>: 线程池实现</li>
</ul>
<h3 id="2-具体模型实现-graph">2. 具体模型实现 (<code>graph/</code>)</h3>
<ol>
<li><strong>ChatGLM系列</strong></li>
</ol>
<ul>
<li><strong>name=&quot;chatGLM.h/cpp&quot; path=&quot;src/graph/chatGLM.h&quot;</strong>: ChatGLM-6B实现</li>
<li><strong>name=&quot;chatGLM2.cpp&quot; path=&quot;src/graph/chatGLM2.cpp&quot;</strong>: ChatGLM2实现</li>
<li><strong>name=&quot;chatGLM3.cpp&quot; path=&quot;src/graph/chatGLM3.cpp&quot;</strong>: ChatGLM3实现</li>
</ul>
<ol>
<li><strong>LLaMA系列</strong></li>
</ol>
<ul>
<li><strong>name=&quot;llama_like.h/cpp&quot; path=&quot;src/graph/llama_like.h&quot;</strong>: LLaMA系列模型的基础实现</li>
<li><strong>name=&quot;ggml_llama.h/cpp&quot; path=&quot;src/graph/ggml_llama.h&quot;</strong>: GGML格式的LLaMA模型支持</li>
</ul>
<h3 id="3-计算内核-kern">3. 计算内核 (<code>kern/</code>)</h3>
<ol>
<li><strong>优化实现</strong></li>
</ol>
<ul>
<li><strong>name=&quot;optimized&quot; path=&quot;src/kern/optimized&quot;</strong>:
<ul>
<li>ARM优化实现</li>
<li>RISC-V向量扩展优化</li>
<li>x86优化实现</li>
</ul>
</li>
</ul>
<ol>
<li><strong>GPU支持</strong></li>
</ol>
<ul>
<li><strong>name=&quot;gpu&quot; path=&quot;src/kern/gpu&quot;</strong>: GPU计算内核实现</li>
</ul>
<ol>
<li><strong>基础实现</strong></li>
</ol>
<ul>
<li><strong>name=&quot;naive&quot; path=&quot;src/kern/naive&quot;</strong>: 朴素CPU实现，作为基准和后备方案</li>
</ul>
<ol>
<li><strong>内核定义</strong></li>
</ol>
<ul>
<li><strong>name=&quot;kernel.h&quot; path=&quot;src/kern/kernel.h&quot;</strong>: 内核接口定义</li>
<li><strong>name=&quot;kernel_define.h&quot; path=&quot;src/kern/kernel_define.h&quot;</strong>: 内核相关常量和类型定义</li>
</ul>
<h3 id="4-工具类-src">4. 工具类 (<code>src/</code>)</h3>
<ol>
<li><strong>文件操作</strong></li>
</ol>
<ul>
<li><strong>name=&quot;file.h/cpp&quot; path=&quot;src/file.h&quot;</strong>: 文件读写和内存映射支持</li>
</ul>
<ol>
<li><strong>通用工具</strong></li>
</ol>
<ul>
<li><strong>name=&quot;utils.h/cpp&quot; path=&quot;src/utils.h&quot;</strong>: 通用工具函数和宏定义</li>
</ul>
<h3 id="主要特点">主要特点</h3>
<ol>
<li><strong>模块化设计</strong></li>
</ol>
<ul>
<li>清晰的层次结构：核心组件、模型实现、计算内核</li>
<li>良好的扩展性：易于添加新模型和优化实现</li>
</ul>
<ol start="2">
<li><strong>多平台支持</strong></li>
</ol>
<ul>
<li>CPU/GPU计算支持</li>
<li>多架构优化：x86、ARM、RISC-V</li>
</ul>
<ol start="3">
<li><strong>性能优化</strong></li>
</ol>
<ul>
<li>针对不同硬件的专门优化</li>
<li>量化支持：INT4/INT8</li>
<li>多线程并行计算</li>
</ul>
<ol start="4">
<li><strong>灵活性</strong></li>
</ol>
<ul>
<li>支持多种模型架构</li>
<li>可配置的计算精度</li>
<li>可扩展的算子系统</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[InferLLM大模型推理框架项目(01)——项目概括]]></title>
        <id>https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-01/</id>
        <link href="https://solahome.github.io/post/InferLLM-Large-Language-Model-infer-framework-project-01/">
        </link>
        <updated>2025-03-13T10:38:14.000Z</updated>
        <content type="html"><![CDATA[<p><a href="https://github.com/MegEngine/InferLLM">InferLLM</a>是一个来自旷视天元<a href="https://github.com/MegEngine">MegEngine</a>的 C++ 大模型推理框架项目，主要参考和借鉴了<strong>llama.cpp</strong>工程。</p>
<h2 id="为什么不选择llamacpp做为大模型推理框架学习入门">为什么不选择<strong>llama.cpp</strong>做为大模型推理框架学习入门？</h2>
<ul>
<li><strong>llama.cpp</strong>核心代码大都在少量文件中，并且使用大量的宏。</li>
<li><strong>InferLLM</strong>对 Kernel 部分进行解耦；定义 KVstorage 类型，方便缓存管理；对多平台进行了优化。</li>
</ul>
<p><strong>InferLLM</strong>是一个 C++ 大语言模型推理框架项目。首先从整体上分析其结构和功能：</p>
<h1 id="inferllm-代码库结构分析">InferLLM 代码库结构分析</h1>
<p>InferLLM 是一个轻量级的 LLM (大型语言模型) 推理框架，主要参考和借鉴了 llama.cpp 工程，但进行了更好的结构化设计。下面是对整个代码库结构的分析：</p>
<h2 id="1-顶层目录结构">1. 顶层目录结构</h2>
<pre><code>InferLLM/
├── application/     # 应用程序目录，包含各种模型的应用实现
├── assets/          # 资源文件
├── docs/            # 文档
├── include/         # 公共头文件
├── src/             # 源代码
├── test/            # 测试代码
├── third-party/     # 第三方依赖
└── tools/           # 工具脚本
</code></pre>
<h2 id="2-核心代码结构-src">2. 核心代码结构 (src/)</h2>
<h3 id="21-核心模块-srccore">2.1 核心模块 (src/core/)</h3>
<p>核心模块包含了框架的基础组件：</p>
<ul>
<li><strong>tensor.h</strong>: 张量定义，是框架的基本数据结构</li>
<li><strong>graph.h</strong>: 计算图定义，包含了模型的结构和执行逻辑</li>
<li><strong>op.h</strong>: 算子定义，实现各种计算操作</li>
<li><strong>kvstorage.h</strong>: KV存储，用于缓存和管理键值对</li>
<li><strong>thread_pool.h</strong>: 线程池实现，用于并行计算</li>
</ul>
<h3 id="22-计算内核-srckern">2.2 计算内核 (src/kern/)</h3>
<p>计算内核部分包含了不同平台的优化实现：</p>
<ul>
<li><strong>naive/</strong>: 朴素实现，作为基准和兜底方案</li>
<li><strong>optimized/</strong>: 针对不同平台的优化实现
<ul>
<li><strong>arm/</strong>: ARM 平台优化</li>
<li><strong>x86/</strong>: x86 平台优化</li>
<li><strong>rvv/</strong>: RISC-V 向量扩展优化</li>
</ul>
</li>
<li><strong>gpu/</strong>: GPU 实现，主要是 CUDA 相关代码</li>
<li><strong>kernel_define.h</strong>: 内核定义和接口</li>
</ul>
<h3 id="23-图结构-srcgraph">2.3 图结构 (src/graph/)</h3>
<p>包含了不同模型的图结构实现：</p>
<ul>
<li>各种模型的具体实现，如 LLaMA、ChatGLM 等</li>
</ul>
<h2 id="3-应用程序-application">3. 应用程序 (application/)</h2>
<p>包含了各种模型的应用实现：</p>
<ul>
<li><strong>chat.cpp</strong>: 通用聊天应用</li>
<li><strong>llama/</strong>: LLaMA 模型应用</li>
<li><strong>chatglm/</strong>: ChatGLM 模型应用</li>
<li><strong>baichuan/</strong>: Baichuan 模型应用</li>
<li><strong>quantizer.cpp</strong>: 量化工具</li>
</ul>
<h2 id="4-测试代码-test">4. 测试代码 (test/)</h2>
<p>测试代码分为 CPU 和 GPU 两部分：</p>
<ul>
<li><strong>CPU/</strong>: CPU 相关测试</li>
<li><strong>GPU/</strong>: GPU 相关测试</li>
<li><strong>checker.h/cpp</strong>: 测试辅助工具</li>
<li><strong>op_create.cpp</strong>: 算子创建测试</li>
</ul>
<h2 id="5-关键数据结构和类">5. 关键数据结构和类</h2>
<h3 id="51-tensor">5.1 Tensor</h3>
<p>张量是基本数据结构，用于表示多维数组数据。</p>
<h3 id="52-graph">5.2 Graph</h3>
<p>计算图是模型的骨架，定义了计算流程和模型结构：</p>
<pre><code class="language-cpp">class Graph : public std::enable_shared_from_this&lt;Graph&gt; {
    // ...
    virtual void construct_llm() = 0;
    // ...
}
</code></pre>
<h3 id="53-oprmodulebase">5.3 OprModuleBase</h3>
<p>算子模块是计算图中的基本单元，实现各种计算操作：</p>
<pre><code class="language-cpp">class OprModuleBase {
    // ...
    virtual void execute(WorkSpace* workspace, uint32_t nr_past, bool is_prefill = false);
    // ...
}
</code></pre>
<h3 id="54-kvstorage">5.4 KvStorage</h3>
<p>KV存储用于缓存和管理键值对，特别是在推理过程中：</p>
<pre><code class="language-cpp">class KvStorage : public Tensor {
    // ...
    void* get_current_data();
    size_t add_id(uint32_t id);
    void reset_id();
    // ...
}
</code></pre>
<h3 id="55-workspace">5.5 WorkSpace</h3>
<p>工作空间用于临时存储计算过程中的数据：</p>
<pre><code class="language-cpp">class WorkSpace {
    // ...
    void* ptr();
    size_t length();
    // ...
}
</code></pre>
<h2 id="6-优化策略">6. 优化策略</h2>
<p>代码库针对不同平台实现了不同的优化策略：</p>
<ol>
<li><strong>ARM 优化</strong>: 使用 NEON 指令集进行向量化计算</li>
<li><strong>x86 优化</strong>: 使用 AVX/AVX2 指令集进行向量化计算</li>
<li><strong>RISC-V 优化</strong>: 使用 RVV 向量扩展进行向量化计算</li>
<li><strong>GPU 优化</strong>: 使用 CUDA 进行并行计算</li>
</ol>
<h2 id="7-模型支持">7. 模型支持</h2>
<p>根据代码库，InferLLM 支持多种模型格式：</p>
<ul>
<li>LLaMA 系列模型</li>
<li>ChatGLM 系列模型</li>
<li>Baichuan 系列模型</li>
<li>支持 int4 量化模型</li>
</ul>
<h2 id="8-主要功能特性">8. 主要功能特性</h2>
<ol>
<li>
<p><strong>多模型支持</strong>：</p>
<ul>
<li>ChatGLM 系列（ChatGLM、ChatGLM2、ChatGLM3）</li>
<li>LLaMA 系列</li>
<li>Baichuan 系列</li>
</ul>
</li>
<li>
<p><strong>多平台优化</strong>：</p>
<ul>
<li>x86 CPU 优化</li>
<li>ARM 处理器优化</li>
<li>RISC-V 向量扩展优化</li>
<li>GPU 加速支持</li>
</ul>
</li>
<li>
<p><strong>工具支持</strong>：</p>
<ul>
<li>模型量化工具</li>
<li>模型转换工具</li>
<li>Android 构建支持</li>
</ul>
</li>
<li>
<p><strong>核心功能</strong>：</p>
<ul>
<li>高效的张量计算</li>
<li>动态计算图</li>
<li>模型推理优化</li>
<li>多线程支持</li>
<li>KV 缓存管理</li>
</ul>
</li>
</ol>
<h2 id="9-项目特点">9. 项目特点</h2>
<ol>
<li>跨平台支持：支持多种硬件平台和架构</li>
<li>模块化设计：核心功能、计算图、内核优化分层清晰</li>
<li>性能优化：针对不同平台提供优化的计算核心</li>
<li>完整的测试框架：包含单元测试和性能测试</li>
<li>实用工具集：提供模型转换和量化等实用工具</li>
</ol>
<h2 id="总结">总结</h2>
<p><strong>InferLLM</strong>是一个结构清晰、模块化设计的 LLM 推理框架，通过解耦框架部分和内核部分，使得代码更易于阅读和修改。它支持多种模型格式，并针对不同平台进行了优化，可以在包括移动设备在内的多种平台上高效运行。</p>
]]></content>
    </entry>
</feed>